# Build History — V1 Skeleton

This is the reconstructed build log for `accommerce-inventory`'s V1 skeleton: what was built, in what order, and — more importantly — every real bug found along the way and how it was fixed. The original session ledger (a live working log kept during the build) was deleted once the branch was finished per process; this document exists so that history isn't lost, reconstructed from git commit messages (which were written to be self-contained) plus the review conversation.

Full detail for *why* each design choice was made lives in [`spec.md`](./spec.md) (the design spec) and [`plan-v1.md`](./plan-v1.md) (the task-by-task implementation plan this build followed). This document is the *what happened*, not the *what was intended*.

## Method

Built via Claude Code's subagent-driven-development process: a fresh implementer subagent per task, a dedicated reviewer subagent after each task (spec compliance + code quality), a fix-and-re-review loop for any Important/Critical finding, then one whole-branch review at the end covering things no single task's reviewer could see. Every task below either passed review clean or went through at least one fix round before being marked complete — nothing shipped with a known Important/Critical defect left open.

## Task-by-Task

**Task 1 — Repo bootstrap.** Express/tRPC/Vite/Vitest scaffold, env loading with fail-fast validation. Fix round: reverted an unnecessary try/catch around `ENV` that would have hidden config errors instead of throwing; fixed a `pnpm-workspace.yaml` placeholder that would have broken a clean install; root-caused a `vitest` cache-busting pattern that doesn't work reliably in this project's actual Vite/Vitest setup, replaced with `vi.resetModules()`.

**Task 2 — Auth primitives.** JWT session tokens, `protectedProcedure`/`editorProcedure` tRPC middleware, cookie-based context. The plan asked this task to also mount the real `/api/auth/*` HTTP routes, but those need the `users` table, which doesn't exist until Task 3 — deferred that piece to Task 3 rather than force a broken ordering.

**Task 3 — Core schema + auth routes.** `users`/`app_settings`/`skus`/`vendors`/`warehouses` tables, plus the `/api/auth/*` routes deferred from Task 2 (password check → list identities → select → session cookie → logout → status). Fix round: the auth routes' DB calls had no error handling — Express 4 doesn't auto-catch async route rejections, so a DB hiccup could hang a request or crash the whole process (taking `/api/trpc` down with it). Added try/catch, verified live by pointing `DATABASE_URL` at a nonexistent host and confirming a clean error instead of a hang.

**Task 4 — `change_log` + `logChange()`.** The generic audit-trail table and helper every later mutating entity uses, with the `reason_category` taxonomy (production delay, artwork delay, customs hold, logistics delay, payment timing, vendor price change, freight rate change, holiday/capacity, other — free-text note required only for "other").

**Task 5 — Purchase Orders.** Status pipeline (draft → confirmed → in production → shipped → customs → delivered → closed) as a lookup table, not branching logic. Planned-ready-date changes require a reason category and log the real prior value.

**Task 6 — Shipments.** Pooled-container SKU-share line items (one shipment can carry a partial, weight/value-allocated share of a PO line item — real Jello containers mix multiple orders). Fix round: `recordShipmentCosts` wrote freight/duty costs with **no audit trail at all** — a real gap against the binding rule that every cost-affecting field change on a Shipment must be logged with a reason; and `markShipmentDeparted` was logging a hardcoded `null` as the "old value" instead of the shipment's real prior departure date. Both fixed and covered by new tests.

**Task 7 — Payments + Transactions.** `fx_rate` captured only at the moment of actual payment (never a standing rate). Fix round: same class of bug as Task 6 — `markPaymentPaid` updated `fxRate`/`paidAmount`/`baseCurrencyAmount` in one write but only audited the `paid` boolean flip, and had no `reason_category` at all despite the schema having a `payment_timing` category built for exactly this case. Fixed to audit all three fields with real prior values.

**Task 8 — `inventory_ledger` + SOH.** The append-only table that is the single source of truth for stock — SOH is always computed live from this table, never stored. Composite index on `(sku_id, warehouse_id, date)` for scale. One real MySQL/Drizzle gotcha found and fixed: `SUM()` over an integer column returns as a JS *string* via the driver used here unless explicitly cast, which would have silently broken every numeric comparison downstream.

**Task 9 — FIFO landed-cost engine.** Pure-function FIFO consumption (oldest batch first) plus per-shipment-line landed unit cost allocation. Fix round: a shipment line item with `qty <= 0` (no positivity constraint existed upstream) would silently produce `Infinity` in a landed-cost figure headed straight for the Money dashboard — added a guard that throws instead.

**Task 10 — Sales plan/actuals, volatility, Daily COGS.** Daily COGS computed as *cumulative-cost-through-today minus cumulative-cost-through-yesterday* — deliberately not per-day-in-isolation, because that's the exact bug class the real Jello Control Tower shipped and had to fix in production. Fix round: `getSalesVolatility` was silently returning the *oldest* N weeks of history instead of the most recent N — a query missing a `desc()` — meaning volatility would freeze on stale data forever once real history exceeded the lookback window.

**Task 11 — Cashflow forecast.** Caught before implementation even started: the plan's own sample code filtered by `expectedDate` for both planned and actual figures, which would silently drop a real payment from `actualOutflow` if it was paid within the query window but originally expected outside it. Rewrote as two independent queries (planned by expected date, actual by paid date) before dispatching the task.

**Task 12 — Daily Shopify pull.** Parses and sums a raw export by SKU+warehouse+date, writes through the same validated `recordSalesActual` path as manual entry — no separate, less-trusted path for automated data.

**Task 13 — Dashboards + tRPC router.** Wired everything into `getHomeSummary`/`getStockDashboard`/`getMoneyDashboard` and the real `appRouter`. Caught before dispatch: the spec commits the V1 Stock dashboard to "SOH, days-of-cover, status thresholds," but the plan's sample code only returned raw SOH — added the days-of-cover calculation (trailing average daily sales → days remaining → critical/low/ok/overstock bucket) before the task even started, and fixed the Home dashboard's stockout-risk count to compare real days-of-cover instead of comparing raw stock units against a day-count threshold (a real unit-mismatch bug in the original sample).

**Task 14 — Frontend shell (Home + Stock).** First frontend task. Two real gaps found before dispatch: `react-router-dom` was never added as a dependency, and the client's tRPC link had no `transformer: superjson` to match the server (would have broken every `Date` field returned by any query). Found live during verification: `vite.config.ts` had no `root`, so the dev server 404'd on every single route — fixed. Fix round: neither page checked `useQuery`'s `error` field, so any real failure (auth expiry, a 500) left the UI stuck on "Loading…" forever with no signal — added error-before-loading to both.

**Task 15 — Purchase Orders + Shipments pages.** Applied the error-before-loading fix from Task 14 to every new query. Found and fixed live: the plan's sample code used one shared `useState` for the reason-category/date/note inputs across the *entire* PO table — meaning editing one row's dropdown visually changed every other row's dropdown too. Moved to per-row state.

**Task 16 — Money section + Change Log.** Found that the router never got a `shipments.list` procedure at all (Task 13 built `getWithLineItems`/`create`/etc. but nothing to enumerate shipments) — without it, the Money page's Landed Cost tab could never get a real shipment to show, and the Shipments page was stuck on a dead stub. Added it. Found live: `MoneyPage`'s inline `new Date(...)` was recomputed fresh on every render, which kept invalidating React Query's cache key and left the page stuck loading forever — fixed via `useMemo`.

**Task 16b — Payments/shipment-cost router (added mid-plan, not in the original 19-task document).** Two independent code reviews confirmed a real usability dead end: the `payments` domain (Task 7) and `recordShipmentCosts` (Task 6) had zero tRPC exposure — no real user could create a payment, mark one paid, or record a shipment's freight/duty cost through the app, only via direct SQL. Added the missing router procedures and a minimal UI (per-shipment cost form, per-PO payment creation/mark-paid). Fix round: the new payment-creation flow didn't invalidate the Money dashboard's cache, so a payment created while Money was already open wouldn't show up until an unrelated remount.

**Task 17 — Migration script.** `transformSheetExport`/`reconcileMigration` (pure, dependency-injected, reports *every* mismatch, never stops at the first) plus `runMigration` (the imperative driver). Sale quantities normalized to negative via `-Math.abs()` regardless of how the source Sheet encodes the sign, so the migration is robust either way.

**Task 18 — Parallel-run comparison report.** Delegates to Task 17's reconciliation logic; `safeToCutOver` is only ever `true` on zero mismatches. Fix round: an *empty* input snapshot also produced zero mismatches — meaning the gate would report "safe to cut over" on a comparison that validated nothing (e.g. if the real Sheet fetch silently failed). Now throws instead.

**Task 19 — Deploy runbook + nightly export (final planned task).** `generateCsvExport` (RFC 4180-style escaping) and `runNightlyExport`, writing one CSV per core table. Fix round: the escaping didn't cover embedded newlines — a real risk given several free-text columns (vendor notes, change-log reason notes) — fixed. `RAILWAY.md` referenced a cron wrapper script that didn't exist; created it, and in doing so found the documented `node --experimental-strip-types` command doesn't actually work against this repo's extensionless relative imports (switched to `pnpm exec tsx`), and that the script never exited on its own because the MySQL connection pool keeps the process alive (added `process.exit(0)`) — both would have made the documented nightly cron job hang or fail on Railway.

## Final Whole-Branch Review

After all 20 tasks, one broad review pass looked across task boundaries for things no single task's reviewer could see. Result: **3 Critical, 13 Important, ~15 Minor findings.**

The 3 Critical findings were genuinely severe — despite every individual piece being correctly built and tested in isolation, the assembled system had no way for an actual human to use it:
- **No login UI and no way to create the first user** — the backend auth flow worked, but nothing in the React app ever called it, and no code path could create a `users` row at all.
- **The production server never served the built frontend** — `pnpm start` ran an API with no UI behind it; the dev-only Vite proxy doesn't exist in production.
- **The Home dashboard's cash-risk figure silently summed payments in different currencies** into one wrong number, with no warning.

Of the 13 Important findings, the review explicitly separated them into "fix now" and "real, but a deliberate follow-up" — see [`BACKLOG.md`](./BACKLOG.md) for the full list and current status of each.

## Fix Wave + Residual

One fix wave addressed the 3 Critical findings plus 3 of the Important ones chosen as directly undermining the shipped product's own stated purpose (login/deploy/currency correctness, a documented-but-broken test command, a real wrong-number bug in the Stock dashboard's headline metric, and the Stock dashboard silently dropping data it already computed). Each fix was independently re-verified — live browser logins, a real `pnpm build` + production server boot, forced-parallel test runs to reproduce the original race, hand-computed regression math — not just re-reading the diff.

The scoped re-review of that fix wave found one residual: the per-bucket currency guard fixed the reported Cashflow bug, but the Home dashboard's *cross-day* sum of multiple currency-clean buckets could still silently blend currencies one level up. Rather than leave that open, the fix was extended once more (at the user's explicit request, not a second automated fix wave) to make the currency-consistency check apply across the *entire* queried range instead of per-day — closing the finding by construction rather than patching each caller separately.

All other Important and Minor findings were deliberately **not** fixed in this round — not silently dropped, tracked in [`BACKLOG.md`](./BACKLOG.md).

## Final State (V1)

- 20 tasks complete (19 from the original plan + Task 16b), each individually reviewed.
- Test suite: 65 tests, 16 files, passing on the plain documented `pnpm test` command (this was itself one of the fixes — the suite silently required a `--no-file-parallelism` flag for the entire build before that was fixed).
- Repository: [github.com/Artem-SCM-AI/accommerce-inventory](https://github.com/Artem-SCM-AI/accommerce-inventory) (private).
- No known open Critical or Important defect in what shipped. A substantial, explicitly-scoped backlog remains for making V1 production-ready against real client data — see `BACKLOG.md`.

---

# Build History — Backlog Stream B: Migration & Cutover Readiness

Built 2026-09-12 to 2026-09-14, directly on `main` (this repo has no branch/worktree workflow — same convention as V1). Design spec: [`2026-09-12-migration-cutover-readiness-design.md`](./2026-09-12-migration-cutover-readiness-design.md). Plan: [`superpowers/plans/2026-09-12-migration-cutover-readiness.md`](./superpowers/plans/2026-09-12-migration-cutover-readiness.md). This is preparatory hardening — **not** a real migration against real Jello/Accommerce data, which remains a separate, later, explicitly-gated decision.

## Method

Same subagent-driven-development process as V1: a fresh implementer subagent per task, a dedicated reviewer subagent after each task, a fix-and-re-review loop for any Important/Critical finding, then one whole-branch review at the end. Baseline before Task 1: 65/65 tests green.

## Task-by-Task

**Task 1 — SKU identifier uniqueness.** Composite unique constraint on `skus(primaryIdentifierType, identifierValue)` via a generated column. Fix round: the migration files `db:push` generated weren't committed (a repo convention this task's own review had to catch); the constraint as first written was bypassable by two rows with the same `primaryIdentifierType` and no value in the corresponding column (both generate `identifierValue = NULL`, and MySQL treats NULLs in a unique index as distinct) — closed by marking the generated column `NOT NULL`.

**Task 2 — Vendor reference + migration-only initial status.** Nullable `vendorReference` on Purchase Orders/Shipments, plus an `initialStatus` escape hatch (used only by migration, never exposed via any router) letting the eventual migration set a real historical status directly instead of always starting at `draft`/`planned`. **Real bug found mid-task, not by trusting the implementer's own report:** the implementer's `db:push` run silently dropped Task 1's composite unique constraint down to a broken single-column one — a MySQL/drizzle-kit quirk where regenerating a STORED generated column drops a dependent constraint without drizzle-kit re-emitting it. The implementer's own report mischaracterized the resulting test failures as "2 pre-existing unrelated" — caught by independently running `SHOW CREATE TABLE` rather than trusting that claim, fixed with a corrective migration.

**Task 3 — Negative-stock validation.** `recordLedgerEvent` throws before writing an event that would drive SOH negative. Implementer hit a session rate limit mid-task; resumed the same agent from its real partial work (correct failing tests already written) once the window passed, rather than restarting. Review: Approved, one Important plan-mandated finding (the date-scoped check only correctly handles chronologically-ordered inserts) carried forward as a binding requirement for the later migration tasks.

**Task 4 — Referential integrity (real foreign keys).** 10 FKs on core ownership edges (PO/shipment line items, payments, transactions, inventory ledger) — previously zero existed anywhere. Required a clean local dev DB reset first. **A second real bug found during this task**, again by not trusting an implementer's "pre-existing, unrelated flake" characterization: `inventory_ledger.date` was a MySQL `TIMESTAMP(0)` (second-precision) column that *rounds* a fractional-second value on insert, compared via `lte()` against a raw, unrounded query parameter — so a receipt and a same-second sale could land on opposite sides of a rounding boundary, silently excluding the receipt from its own SOH-as-of check and (combined with Task 3's new guard) rejecting a legitimate write. Root-caused via a live DB inspection and a standalone reproduction script, confirmed present at the pre-Task-4 commit too (via a temporary git worktree) — fixed with `timestamp(3)` (millisecond precision). One further fix round: the FK-safe test cleanup pattern (`SET FOREIGN_KEY_CHECKS = 0/1` across 10 test files) had no `try/finally`, so one failing delete would leave FK enforcement silently disabled for the rest of that connection's life.

**Task 5 — Shipment status state machine.** `VALID_SHIPMENT_TRANSITIONS`, mirroring the existing PO pattern; `markShipmentDeparted` now validated through it instead of hard-setting status. Not exposed via any router in this task. Fix round: a real test-coverage gap (no test exercised the new transition-table branch specifically, both existing tests hit an older guard first). One finding ruled out of scope rather than fixed: the new transition table permanently closes the "call `markShipmentDeparted` twice to fix a wrong date" path with no replacement built — a deliberate scope boundary for hardening-only work, flagged forward to Backlog Stream A.

**Task 6 — Already-matched-transaction guard.** App-level check in `matchTransactionToPayment` (the FK from Task 4 only catches a reference to a payment that doesn't exist at all, not "already matched to a different payment"). Fix round: a nonexistent transaction ID crashed with an opaque `TypeError` instead of a clear error — closed with an explicit existence check.

**Task 7 — Migration transform functions.** Pure transforms (`transformPurchaseOrders`/`transformShipments`/`transformPayments`/`transformTransactions`) converting raw Sheet-row shapes into typed objects Task 8 consumes by exact field name — reviewed with extra scrutiny on field-name fidelity since Task 8 was already written against these exact names. Two fix rounds: `parseFloat`/`parseInt` accepted leading-numeric garbage (`"123abc"` → `123`) instead of quarantining across all four transforms (plus a second round after the fix initially missed `transformTransactions`, caught by the implementer itself flagging the scope gap rather than silently leaving it).

**Task 8 — Widen `runMigration`.** The biggest task: full PO/Shipment/Payment/Transaction scope, wrapped in one atomic DB transaction. Resolved a placeholder the plan had deliberately left open (how to migrate a shipment's historical freight/duty costs without `recordShipmentCosts`'s audit-trail requirement) before dispatch, rather than leaving it for the implementer to invent. Implementer self-reported and the reviewer independently verified a genuinely necessary architectural finding: Drizzle's `db.transaction()` provides a scoped client, and any write going through the outer pool-backed client instead doesn't participate in the transaction at all — required threading an optional `dbClient` parameter through 9 functions across 5 files to make the atomicity guarantee real rather than illusory. Approved clean, no fix round.

**Task 9 — Landed-cost tolerance reconciliation.** Tolerance-based check (greater of $0.01 or 0.1%) alongside the existing exact-match SOH check. Deliberately built the machinery without wiring it to real data (real Sheet landed-cost columns are unknown) — approved clean.

**Task 10 — CLI entrypoints (final planned task).** `scripts/run-migration.mjs`/`run-parallel-check.mjs`. Implementer caught that the plan's own sample code for the parallel-check script was stale (assumed `deps`, a function-containing object, could be read from JSON) and correctly built it from live DB queries instead — verified by the reviewer against the actual pre-existing code, not taken on faith.

## Final Whole-Branch Review

One broad review pass, dispatched on the most capable available model, looked across all 10 tasks' combined diff for cross-cutting issues no single task's reviewer could see. Result: **2 Critical, 6 Important findings** — every one a genuine interaction between tasks built independently:

- Landed-cost reconciliation (Task 9) was built and tested but never actually reachable from `runMigration` (Task 8) — only 3 of the function's 4 arguments were passed, so the check silently ran zero comparisons while reporting success.
- The committed migration history contained an unconditional `DELETE FROM skus` (the fossil record of Task 2's mid-build fix), harmless on the empty database every real deployment starts from but a landmine for any future non-empty one.
- Task 8's transaction-atomicity fix didn't reach `recordSalesActual` (untouched V1 code) — Task 3's new guard could now throw there too, leaving an orphaned `sales_actuals` row with no matching ledger event.
- `runMigration` didn't sort ledger events by date before replay, exposing exactly the ordering sensitivity Task 3's own review had flagged and deferred.
- Dangling cross-entity references (a shipment pointing at a quarantined PO's line item, a payment pointing at a quarantined PO) were handled two different wrong ways — one crashed the whole migration, one silently orphaned data.
- A PO line-item lookup relied on unordered `SELECT` row order plus a key that collides on duplicate SKUs within one PO.
- The FK-safe test cleanup pattern (Task 4) was connection-pool-based, not connection-pinned — worked only by the accident of sequential test execution.
- The original V1 ledger transform was never given the same quarantine treatment Task 7 gave its four newer siblings — the direct cause of `quarantined.ledger` being permanently empty.

## Fix Wave + Residual

One fix wave addressed all 8 findings — dispatched as a single subagent with every resolution pre-decided by the controller (not left for the implementer to improvise architecture under time pressure), given the scope and design judgment several of them required (especially the migration-history squash and the atomicity/ordering/quarantine fixes). First dispatch attempt hit a model rate limit before any work began; redispatched cleanly with no lost work. Result: 4 focused commits, 111/111 tests, clean typecheck.

Independently verified by the controller before the scoped re-review — not just the test suite, but the actual squashed migration SQL (confirmed zero destructive statements) and live `SHOW CREATE TABLE` output for the affected tables — a direct application of the lesson learned during Task 2's own incident earlier in this same build. Scoped re-review confirmed all 8 findings addressed with no new breakage; two of them (an `ORDER BY` fix and a connection-pinning fix) were accepted without dedicated new regression tests, on the reasoning that a meaningful isolated test for either would require inducing undefined database row-ordering or mocking connection-pool internals — disproportionate to fixes that size, and both are exercised indirectly by the full suite on every run.

## Final State (Stream B)

- 10 tasks complete, each individually reviewed; one whole-branch review found and fixed 2 Critical + 6 Important cross-task issues.
- Test suite: 111 tests, 17 files, passing on the plain documented `pnpm test` command.
- No known open Critical or Important defect. Five smaller items surfaced by this stream's own build are tracked, not silently dropped — see `BACKLOG.md` section B.

---

# Build History — Backlog Stream A: Wiring Completion

Built 2026-09-19, directly on `main` (same convention as V1 and Stream B). Design spec: [`2026-09-19-wiring-completion-design.md`](./2026-09-19-wiring-completion-design.md). Plan: [`superpowers/plans/2026-09-19-wiring-completion.md`](./superpowers/plans/2026-09-19-wiring-completion.md). Exposes seven pieces of already-built-and-tested backend logic — plus four small write functions and two small read functions a pre-implementation code survey found were also genuinely missing — through real router procedures and minimal UI, closing the original V1 review's "logic exists, nothing reaches it" finding.

## Method

Same subagent-driven-development process as V1 and Stream B. Baseline before Task 1: 111/111 tests green. One methodological shift discovered mid-Task-1 and used for the rest of the build: subagents dispatched via the Agent tool cannot actually interact with a browser, despite repeated explicit instructions to do so — two different implementer instances both substituted "predicted" behavior for "observed" behavior on the first ask. Rather than keep demanding an impossible verification, the controller found this app's real, scriptable login flow (`/api/auth/password` → `/api/auth/users` → `/api/auth/select-user`, session cookie) and personally verified every task's actual runtime behavior via direct HTTP requests against a running server for the remainder of the build — a stronger, more concrete check than any subagent's browser claim could have provided.

## Task-by-Task

**Task 1 — Sales plan entry.** New `createSalesPlanEntry` insert path (the spec's plan-vs-actual and volatility reads already existed and were tested; only the write side was missing) + router + a new Stock page section. Fix round caught two real issues: an implementer's "live browser verification" claim was overclaimed and internally self-contradictory (asserted success while admitting in the same report that a specific claimed behavior "would require manual testing" — and that behavior wasn't even in the code). Also closed a real plan-mandated gap: the new UI silently swallowed catalog-load errors instead of surfacing them, against this codebase's explicit error-handling convention.

**Mid-build, unrelated to any task**: `pnpm test` suddenly reported 224 tests instead of 112 — investigated immediately rather than trusted. Root cause: a concurrent, independent session (the user's own parallel Fable-model session, exploring separately) had committed a `.gitignore` change straight to shared `main` from its own git worktree, and vitest's recursive glob was picking up that worktree's full copy of every test file. Fixed with a `vitest.config.ts` exclude, confined to this checkout, without touching the other session's worktree in any way.

**Task 2 — Shipment status progression.** Wired the existing (Stream-B-built, previously unexposed) `updateShipmentStatus` to a router + Shipments page UI, extending it with an optional `reasonCategory`/`reasonNote` to match the Purchase Order equivalent's existing shape — a real inconsistency flagged but deliberately left unfixed during Stream B's own review, closed here now that the function is finally reachable. Approved clean. First task independently verified end-to-end via the real HTTP method described above: a real shipment's status was moved planned→departed via the actual API, with the `change_log` audit row confirmed correct.

**Task 3 — Customs status + actual arrival date.** Two new functions (`setShipmentCustomsStatus`, `markShipmentArrived`) — the schema columns had existed since V1 with nothing ever writing to them. Implementer honestly reported its verification method this time (typecheck + code read, no browser claim). Approved clean; all Minor findings traced to pre-existing file-wide patterns, not this task's to fix.

**Task 4 — Actual depart date correction.** The most safety-critical task in this plan: a dedicated `correctShipmentActualDepartDate` bypassing the shipment transition table by design, with a required null-guard preventing it from becoming an unvalidated backdoor around the state machine Stream B built. Controller proved both sides of the guard via real API calls (rejection on a never-departed shipment; success correcting a genuinely-departed one) before dispatching review. Approved clean.

**Task 5 — Payments history (pure wiring).** `payments.history` calling the existing generic `listChangeLog`. Verified end-to-end: a real payment was marked paid, generating 3 real audit rows, all correctly returned.

**Task 6 — Payments re-listing.** New `listPaymentsForPo`, fixing a real bug where payments existed in the database but vanished from the UI on page reload (previously session-local React state). Carried a flagged cross-task coupling risk from pre-flight planning — Task 5 had added a `<PaymentHistory>` element inside a component this task needed to change the signature of — which resolved cleanly with no regression, confirmed independently by the controller, the implementer, and the task reviewer all separately.

**Task 7 — Shipments linked to a Purchase Order (pure wiring).** `shipments.listForPo` composing two existing tested functions. Approved clean, zero findings.

**Task 8 — Transaction matching UI (final planned task).** New `listUnpaidPayments`, wiring the already-existing (but never UI-exposed) `matchTransactionToPayment` to a real manual-pick dropdown on the Money page. Fix round: an N+1-shaped query (each row independently querying instead of the parent fetching once) — not a functional bug given react-query's caching, but a real inconsistency with this file's own established data-fetching convention, closed properly.

## Final Whole-Branch Review

One broad review pass, dispatched on the most capable available model, looked across all 8 tasks' combined diff for cross-cutting issues. Result: **2 Critical, 2 Important findings**, both Critical ones independently confirmed by the controller against the live code before any fix was dispatched:

- The new UI's only path to a "departed" shipment status called the wrong function (`updateShipmentStatus` instead of `markShipmentDeparted`), producing a shipment permanently stuck with a null, unfixable actual depart date — the exact class of dead-end this whole plan exists to prevent, recreated from the opposite direction. Task 4's own null-guard, working exactly as designed, is what made this state unrecoverable rather than merely wrong.
- A genuine React Rules-of-Hooks violation in Task 1's own fix-round code (not the original task), guaranteed to crash the Stock page on any catalog query error.
- The new transaction-matching UI could silently double-match one payment to two transactions, with no undo path anywhere in the codebase — the existing match guard only checked the transaction side, never the payment side.
- Three of the plan's four new write functions omitted the mandated `dbClient` threading the other three correctly had.

## Fix Wave + Residual

One fix wave addressed all 4 findings: a server-side guard rejecting the wrong departure path (with the client's misleading button removed, but no new "mark departed with a date" feature built — that gap predates this entire stream and is now explicitly tracked in `BACKLOG.md` rather than hastily filled), the hooks-order fix, a payment-side double-match guard plus dropdown filtering and PO-number disambiguation, and the `dbClient` threading. 3 commits, 124/124 tests, clean typecheck.

Every fix was independently verified by the controller directly against the code — not the report — before the scoped re-review, including confirming the subtle distinction that the SERVER's own shipment transition table correctly still lists `planned → departed` (required by `markShipmentDeparted` itself) while only the CLIENT's copy needed the entry removed. Scoped re-review confirmed all 4 findings addressed with no new breakage, including hand-tracing the new double-match guard against the pre-existing idempotent-rematch test to confirm no regression.

A deliberately deferred Important finding (a pre-existing full-table-scan query pattern, multiplied by this stream's own new per-row UI usage) and roughly seven Minor findings are tracked in `BACKLOG.md`, not silently dropped.

## Final State (Stream A)

- 8 tasks complete, each individually reviewed; one whole-branch review found and fixed 2 Critical + 2 Important cross-task issues.
- Test suite: 124 tests, 17 files, passing on the plain documented `pnpm test` command.
- No known open Critical or Important defect. All seven originally-planned wiring items are done; the backend logic identified in V1's original review as "built, tested, unreachable" is now fully reachable.
- One real, pre-existing gap surfaced (not introduced) by this stream is now explicitly tracked rather than silently left: no UI exists anywhere to set a shipment's planned depart date or mark it departed with a real date — closed off from being reachable through the wrong function, but the correct UI for it remains unbuilt.
- Landed-cost reconciliation machinery exists and is tested but is deliberately not wired to a real data source (unknown real Sheet columns) — it fails loudly rather than silently if asked to run, per the spec's "fail loudly, never silently" principle.
- Migration history was squashed to one clean, non-destructive migration before this repo was ever deployed anywhere — the right and only safe moment to do it.

# Build History — Backlog Stream E: Systemic Correctness Hardening

Built 2026-09-19, directly on `main` (same convention as V1, Stream B, and Stream A). Design spec: [`2026-09-19-systemic-correctness-hardening-design.md`](./2026-09-19-systemic-correctness-hardening-design.md). Plan: [`superpowers/plans/2026-09-19-systemic-correctness-hardening.md`](./superpowers/plans/2026-09-19-systemic-correctness-hardening.md). Closes three correctness risks V1's original review flagged as "not urgent today because no real production data has flowed through yet — will matter the moment it does": a non-idempotent daily Shopify pull, systemic date/timezone drift across four calendar-day columns, and an unenforced landed-cost currency assumption.

## Method

Same subagent-driven-development process as V1, Stream B, and Stream A. Baseline before Task 1: 124/124 tests green. A pre-design code survey found a real conflict between the backlog's own generic recommendation (convert all four calendar-day columns to Drizzle `date` string mode) and a fix Stream B had already shipped: `inventory_ledger.date` was deliberately upgraded to `timestamp(3)` to fix a real same-day event-ordering bug, and a date-only column can't express "this event happened before that one, same day." The approved design split accordingly — three columns convert, `inventory_ledger.date` instead gets a pinned-UTC connection-timezone fix.

## Task-by-Task

**Task 1 — Landed-cost currency guard.** `getShipmentLandedUnitCost` gains a check rejecting a PO-line/shipment currency mismatch. Task review caught a real Critical bug in the first draft: `shipment.costCurrency` is genuinely nullable by design ("no real invoice yet"), and the guard's naive `!==` check treated that null as a mismatch, turning a previously-valid EXW-only cost computation into a spurious throw, reachable via the Money dashboard's shipment filter. Fixed by only enforcing the check when `costCurrency` is non-null, with a regression test proving the null case still works. One fix round, clean re-review.

**Task 2 — Pin UTC timezone on the MySQL connection pool.** The single most significant technical finding of the whole stream: the plan's own specified mechanism (a mysql2 `timezone: "Z"` pool option) turned out to be **completely ineffective** for this codebase, caught by the task reviewer empirically comparing pinned vs. unpinned pools against the real dev DB and independently re-confirmed by the controller reading the actual installed source of `drizzle-orm` and `mysql2`. Root cause: Drizzle's own `timestamp()` column mapper unconditionally forces UTC interpretation regardless of any connection setting, and Drizzle's mysql2 session override bypasses mysql2's own timezone-aware parser entirely for TIMESTAMP/DATETIME/DATE fields — the `timezone` pool option was purely client-side parsing that never even ran. This was ruled a correction to the *design's own stated mechanism*, not just an implementation bug: the intent (pin the real database session to UTC) was correct and binding, the specified technical means was simply wrong. Corrected fix: `pool.pool.on("connection", ...)` issuing a real `SET time_zone = '+00:00'` command on every new physical connection, verified via a real insert-then-readback round-trip against this machine's actual non-UTC system timezone (Europe/Kiev, confirmed live). A second fix round added an error callback to the `SET` command after a re-reviewer flagged that a failed command would otherwise crash the whole process via mysql2's unhandled-error-event behavior.

**Task 3 — `purchase_orders.plannedReadyDate` → calendar-day string.** Schema, function signatures, and the tRPC router boundary conversion (wire stays `z.date()`, handler converts to `"YYYY-MM-DD"`) all changed together; the client's shared `toDateInputValue` helper widened to accept both types without disturbing its other, still-`Date`-typed call sites. Reviewed clean, no fix round — including confirming Drizzle's `date` (string) mode has no hidden timezone landmine analogous to Task 2's (its `MySqlDateString` type has no driver-value transform methods at all).

**Task 4 — `sales_plan.periodDate` / `sales_actuals.date` → calendar-day strings.** Same conversion pattern as Task 3, plus a genuinely necessary internal fix: `recordSalesActual`'s call into the untouched `recordLedgerEvent` needed an explicit `new Date(input.date)` conversion back to a real `Date`. Task review caught a second real, empirically-reproduced bug: the brief's own specified conversion anchored the derived ledger "sale" event at UTC midnight, which meant a same-day `receipt` recorded at a real, later wall-clock timestamp fell *after* the midnight-anchored sale in the negative-stock guard's comparison — spuriously rejecting a perfectly valid same-day receipt-then-sale sequence. Ruled a correction to the brief's own specified conversion (same pattern as Task 2): since `sales_actuals` rows are whole-day aggregates, not point-in-time transactions, the correct anchor is END of day (`23:59:59.999Z`), which also required switching `getDailyCogs`'s date filters from raw timestamp comparison to calendar-day string comparison. One fix round, re-review independently re-derived the fix's correctness by hand rather than trusting the report.

**Task 5 — Daily Shopify pull idempotency.** A unique constraint on `sales_actuals(skuId, warehouseId, date, source)` plus a pre-insert existence check in `runDailyShopifyPull`, quarantining a duplicate into `skipped` with a reason rather than re-importing it — matching this codebase's established quarantine-not-abort convention. Reviewed clean, no fix round; new test proves no double-counting on both the `sales_actuals` and `inventory_ledger` row counts, not just the summary counters.

**Task 6 — Daily Shopify pull CLI entrypoint (final planned task).** `scripts/run-daily-shopify-pull.mjs`, pure wiring matching the existing `run-nightly-export.mjs` script's conventions. Reviewed clean; one judgment call (consolidating the brief's two try/catch blocks into one, matching the actual convention of sibling scripts) accepted as a justified, functionally-equivalent deviation.

## Final Whole-Branch Review

One broad review pass, dispatched on the most capable available model (run in the background given the diff's size), looked across all 6 tasks' combined diff for cross-cutting issues no single task-scoped reviewer could see. Result: **0 Critical, 6 Important findings**, several independently re-verified and empirically reproduced by the controller (not just read) before routing the fix wave:

- The negative-SOH guard, checking solvency against an event's own exact timestamp, could miss a same-day event inserted with an earlier real timestamp than that day's end-of-day-anchored sale (Task 4's own fix) — reproduced live: receipt, day-close sale, then a same-day correction with an earlier timestamp was wrongly accepted, landing the ledger at a real negative SOH. Not reachable via any function that ships negative-qty writes today, but a live risk the moment any correction/returns path is added — exactly the same-day-ordering class Stream B's `timestamp(3)` fix exists to prevent.
- `getMoneyDashboard` called `getShipmentLandedUnitCost` (Task 1's new guard) unguarded, so one currency-mismatched shipment blanked the entire Money page — Cashflow, Daily COGS, and transaction-matching, not just the Landed Cost tab — invisible to Task 1's own reviewer, who only saw `landedCost.ts` and its test.
- `pnpm check` failed on HEAD — Task 2's timezone test didn't typecheck against `db.execute`'s actual return type. Vitest doesn't typecheck, so no per-task review caught it.
- `getDailyCogs` took a `Date` for a calendar-day-only value and truncated it internally, violating the plan's own stated constraint; `dashboards.ts`'s date-enumeration helper mixed local-time stepping with UTC-based keying — the exact drift class this whole stream exists to close, left open in a file the stream itself touched.
- The design's mandated `inventory_ledger` insert-then-read-back round-trip test was replaced during Task 2's fix loop with a weaker session-timezone-only check, proving the `SET` command ran but not that Drizzle's value round-trip is actually correct under it.
- `runDailyShopifyPull` had no per-row error handling — one row failing downstream (insufficient stock, a malformed date) threw and aborted the *entire* batch, silently losing every other valid row and returning no summary at all, contradicting this codebase's own quarantine-not-abort convention that Task 5 was explicitly written to follow.

## Fix Wave + Residual

One fix wave addressed all 6 findings, each independently verified by the controller against the actual current code before dispatch, not accepted on the reviewer's word alone: the negative-SOH guard now evaluates solvency as of the end of the triggering event's own calendar day rather than its exact timestamp, making same-day events mutually visible regardless of insertion order; the Money dashboard degrades a currency-mismatch throw to an error field scoped to just the Landed Cost tab; the typecheck failure was fixed with a narrowly-scoped double-cast after confirming drizzle-orm's mysql2 driver genuinely hardcodes its `execute()` return type regardless of the generic passed; `getDailyCogs` now takes a calendar-day string directly and the date-enumeration helper was rewritten to use only UTC-based date arithmetic; a real round-trip test was added proving the UTC pin's value survives a full write/read cycle (independently confirmed load-bearing on this exact machine, whose real system timezone is UTC+3); and the daily pull's per-row body was wrapped in a try/catch that quarantines a downstream failure instead of aborting the batch. 4 commits, 131→135/135 tests, `pnpm check` clean.

Scoped re-review independently hand-traced each fix's before/after logic rather than trusting the fix report — including re-deriving the exact guard arithmetic that would have wrongly accepted the pre-fix reproduction, reading drizzle-orm's own HKT type source to confirm the typecheck fix's library-behavior claim, and querying this dev server's real system timezone to confirm the new round-trip test is a genuine regression guard on this machine, not a vacuous one. All 6 addressed, no new findings.

Several Minor findings were parked with a ruling rather than fixed or silently dropped — tracked in `BACKLOG.md` section E: a per-source idempotency gap (the unique constraint's `source` column means a future `manual`-source write could still double-deplete stock alongside a Shopify-sourced one; no such write path exists today), a mismatched sale-event anchor time between live code and the (migration-only) `reconcile-migration.ts` replay script, a `change_log` date-format inconsistency between entity types, a case-sensitive currency comparison in the new guard, and the new CLI script pulling unfiltered (including inactive) SKUs into its lookup table.

## Final State (Stream E)

- 6 tasks complete, each individually reviewed; two tasks (2 and 4) each required one fix round that corrected the plan's own specified technical mechanism, not just an implementation slip — both independently verified by the controller against real library source and a live database before being accepted. One whole-branch review found and fixed 6 Important cross-task issues in one wave, no Critical findings at any stage.
- Test suite: 135 tests, 17 files, passing on the plain documented `pnpm test` command. `pnpm check` (`tsc --noEmit`) passes clean.
- All three of V1's originally-flagged systemic correctness risks are closed: the daily Shopify pull is idempotent with a real CLI entrypoint, the four calendar-day columns' timezone-drift risk is closed (three converted to a real date type, `inventory_ledger.date`'s Stream-B precision fix preserved via a pinned UTC session), and the landed-cost currency assumption is enforced.
- A genuinely latent same-day event-ordering bug — introduced by this stream's own necessary anchor-time choice for aggregate sale events, not present before it — was found and closed within the same stream, not shipped and deferred: the negative-stock guard now evaluates same-day solvency correctly regardless of which event was inserted first.
- No known open Critical or Important defect. Five small, genuinely deferred items are tracked in `BACKLOG.md` section E, none load-bearing for the platform's core correctness guarantees.

# Build History — Backlog Stream C: Security Hardening

Built 2026-09-20, directly on `main` (same convention as V1, Stream B, Stream A, and Stream E). Design spec: [`2026-09-20-security-hardening-design.md`](./2026-09-20-security-hardening-design.md). Plan: [`superpowers/plans/2026-09-20-security-hardening.md`](./superpowers/plans/2026-09-20-security-hardening.md). Replaces the shared-`APP_PASSWORD`-plus-identity-picker login flow — anyone who knew the one shared password could select ANY user identity, including the sole editor account with full write access to real financial data — with real per-user credentials, session revocation, a working sign-out control, and CLI-based user/password provisioning.

## Method

Same subagent-driven-development process as the prior three streams. Baseline before Task 1: 135/135 tests green. This stream's design phase differed from every prior one: the user, by their own account, could not technically review a security-architecture spec ("Я ж не шарю в цьому" — "I don't understand this stuff") and explicitly delegated that review back to the controller rather than approving it themselves. Before locking the design in, the controller ran a genuine three-lens self-critique pass (methodical rigor, product simplicity, adversarial skepticism) against its own draft — the adversarial lens surfaced a real gap the first draft had left as an explicit Non-Goal (no rate-limiting on the login endpoint), and the design was revised to add a minimal per-email failed-login throttle before being committed, rather than shipping the gap and discovering it later.

`jose` (JWT signing) was kept throughout — the design's own analysis found it was never the problem; the flow built around it (a single shared secret gating a free pick of any identity) was.

## Task-by-Task

**Task 1 — Password hashing.** New `server/_core/passwords.ts`: `hashPassword`/`verifyPassword` via Node's built-in `crypto.scrypt` (no new npm dependency), random per-password salt, `timingSafeEqual` comparison, fails closed on null/malformed stored hashes. Reviewed clean, no fix round.

**Task 2 — Failed-login throttle.** New `server/_core/loginThrottle.ts`: an in-process, per-email state machine — 5 failures within a 15-minute counting window triggers a 15-minute lockout; a retry during an active lockout does not extend it (preventing a scripted retry loop from keeping a legitimate user locked out indefinitely). The task reviewer hand-traced every state transition, including the trickiest one, and confirmed the test suite genuinely discriminated correct from subtly-buggy behavior rather than merely reading as plausible. Reviewed clean, no fix round.

**Task 3 — Core login flow rewrite.** The largest task, deliberately not split further: `auth.ts`'s signature change (a JWT payload gaining `tokenVersion`) and `authRoutes.ts`'s route rewrite are irreducibly coupled at the type level, so splitting them would have left the suite red between tasks. Schema gains `users.passwordHash`/`tokenVersion`; a new `loginFlow.ts` becomes the single DB-touching choke-point for auth logic (`attemptLogin`/`resolveSession`/`performLogout`), consumed identically by both `context.ts` (tRPC) and `authRoutes.ts` (plain Express) rather than each reimplementing the revocation check; the old `/api/auth/password`/`users`/`select-user` flow is deleted entirely in favor of one `/api/auth/login`; session lifetime drops from 365 to 30 days. The implementer independently caught and fixed a real type bug in the plan's own literal `context.ts` code (a field-name mismatch that `tsc` would have caught immediately). The task reviewer then found a genuine availability regression, also inherited from the plan's own literal code rather than introduced by the implementer: none of the three new Express auth handlers wrapped their calls in try/catch, so a transient DB error would — by Node's default behavior on an uncaught promise rejection inside an async Express 4 handler — crash the *entire server process* over one bad login attempt, not just fail that one request. Confirmed as a genuine regression (the pre-Task-3 code had this protection) and as a violation of the plan's own Global Constraints ("fails closed... never a crash"), not just a nice-to-have. One fix round added the missing try/catch plus this codebase's first-ever HTTP-level test (a real Express app on a real port, `vi.mock`-ing the DB-touching layer to simulate a failure) — self-validated by the implementer temporarily reverting the fix and confirming the new test then genuinely failed.

**Task 4 — Client login form and sign-out button.** `LoginPage.tsx` rewritten to a single email+password form; `AppNav.tsx` gains a real "Sign out" control using try/finally so a network failure never leaves a user stuck mid-flow. Reviewed clean, no fix round; verified via this codebase's established scriptable-HTTP-flow method (no client-side test suite exists in this repo).

**Task 5 — User provisioning scripts and documentation (final planned task).** `scripts/seed-first-user.ts` extended to require and hash a password; new `scripts/resetPassword.ts`/`reset-password.mjs` reset a password AND atomically bump `tokenVersion` in the same update (avoiding the lost-update race a read-then-write would have reintroduced); `RAILWAY.md`/`.env.example`/`README.md` updated to remove the now-dead `APP_PASSWORD` references. Reviewed clean, no fix round.

## Final Whole-Branch Review

One broad review pass, dispatched on the most capable available model (run in the background given the diff's size and this stream's security stakes), looked across all 5 tasks' combined diff for cross-cutting issues no single task-scoped reviewer could see. Before dispatching it, the controller had already caught a one-time test-suite flake after Task 5 landed (`1 failed | 21 passed`, error message lost before it could be captured) — 25 additional consecutive clean runs couldn't reproduce it, so the final reviewer was explicitly charged with running it down further rather than letting an unexplained intermittent failure ship silently in a security-critical stream. The reviewer ran 80 more runs (including 10 with test order shuffled) with zero reproductions, and — reading the actual `@trpc/server` library source rather than assuming — separately confirmed that a second, parallel crash-the-process exposure the reviewer was specifically asked to check for (whether `context.ts`'s `createContext`, which calls the same risky `resolveSession` function, had the same gap the Task 3 fix closed only in `authRoutes.ts`) does *not* exist: tRPC's own request pipeline catches a `createContext` rejection in two independent layers, confirmed by reading the installed library's actual source rather than assumed.

The review found **1 Critical, 4 Important findings**, the Critical one demonstrated live against the real dev database rather than merely reasoned about:

- **The failed-login throttle — this stream's only anti-brute-force control, and the design's own stated answer to "unlimited password guesses" against a login endpoint protecting real financial data — was bypassable by varying the submitted email's letter case.** `loginThrottle.ts` keyed its state on the raw string while the database matches emails case-insensitively; the reviewer proved live that 5 failures on one casing left every other casing of the same email fully unlocked, giving an attacker effectively unlimited guesses via case permutation. A second face of the same root cause: production TiDB's likely different default collation could instead cause a *legitimate* login to fail for a differently-cased but correct email — a real dev/prod behavioral divergence baked into the unnormalized code.
- The throttle's in-memory map had no bound on the number of distinct emails it could track, and no cap on a submitted email's length — an unauthenticated memory-exhaustion path on an internet-facing endpoint, distinct from the design's own accepted "no distributed rate limiting" scope decision.
- `docs/BACKLOG.md` section C and `README.md` — live decision documents, not historical records — still described the exact vulnerability this stream fixed as if it were still current. A plan-level gap: Task 5's brief asked for `RAILWAY.md`/`.env.example`/one README line, never for `BACKLOG.md`.
- The design's own mandated byte-for-byte anti-enumeration regression test didn't exist, and cookie attributes (httpOnly/sameSite/30-day-parity-with-the-JWT) had zero test coverage — today's behavior was correct, but nothing would catch a future one-line regression of either property.
- All three `authRoutes.ts` catch blocks were silent (no logging at all) — `/api/auth/logout` in particular could tell a client "signed out" while a failed server-side revocation left their token fully valid, with zero trace in production logs, contradicting this project's "fail loudly, never silently" principle.

## Fix Wave + Residual

One fix wave addressed all 5 findings, each independently verified by the controller against the actual current code and, for the Critical finding, the live dev database — not accepted on the reviewer's or implementer's word alone: a `normalizeEmail` (trim + lowercase) helper is now applied consistently everywhere an email is used as a lookup key or stored (the login flow, the seed script, the reset script), closing both the throttle bypass and the dev/prod behavioral divergence in one fix; the throttle's map gained a 10,000-entry cap with oldest-first eviction; `BACKLOG.md`/`README.md` were brought current; a new unmocked HTTP-level test file proved byte-identical failure responses across three enumeration-sensitive cases and correct cookie attributes; all three catch blocks gained route-specific `console.error` logging with the response contract otherwise unchanged. 5 commits, 161→167/167 tests, `pnpm check` clean.

The scoped re-review found 4 of the 5 fixes genuinely correct on inspection, and caught one real residual issue in the fix diff itself: the new map-eviction regression test had an off-by-one between its loop bound and its own code comment's stated scenario, making it pass identically whether the eviction fix worked or was completely disabled — a vacuous test for a security-relevant control, though the underlying production fix was independently confirmed correct by both the re-reviewer and the controller. Per this stream's own process (no second automated fix-wave dispatch at the final-review stage), the controller adjudicated this directly rather than spinning up another subagent cycle for a one-line, mechanically-verifiable test correction — applying the fix, then personally proving it discriminates by temporarily disabling the real eviction code and confirming the corrected test now genuinely fails, before restoring the real code and re-confirming the full suite green.

No Minor findings from this stream's final review were fixed; nine were parked and are tracked in `BACKLOG.md` section C's deferred-items list, none load-bearing.

## Final State (Stream C)

- 5 tasks complete, each individually reviewed; one task (3, the core rewrite) required one fix round that closed a real availability regression inherited from the plan's own literal code, independently verified as a genuine regression via git history rather than assumed. One whole-branch review found and fixed 1 Critical + 4 Important cross-task issues in one wave, with one further controller-adjudicated correction to the fix wave's own test coverage.
- Test suite: 167 tests, 23 files, passing on the plain documented `pnpm test` command. `pnpm check` (`tsc --noEmit`) passes clean.
- The shared-password role-boundary hole flagged in V1's original review is closed: real per-user credentials mean a viewer can never authenticate as the editor. Session revocation, a 30-day session lifetime, a working sign-out control, and a (bounded, case-normalized) failed-login throttle are all real and tested, not just designed.
- A genuine security regression from an earlier stream's plan-writing — an unhandled-DB-error crash path with no defensive coding at all, where the code it replaced had some — was found and closed within this same stream, not shipped and deferred, matching the pattern already established in Streams B/A/E of catching and closing gaps introduced by the stream's own work rather than only the gaps it set out to fix.
- No known open Critical or Important defect. Nine small, genuinely deferred items are tracked in `BACKLOG.md` section C, none load-bearing for the login/session security properties this stream exists to guarantee.

# Build History — Backlog Stream D: Performance Hardening

Built 2026-09-20, directly on `main` (same convention as every prior stream). Design spec: [`2026-09-20-performance-hardening-design.md`](./2026-09-20-performance-hardening-design.md). Plan: [`superpowers/plans/2026-09-20-performance-hardening.md`](./superpowers/plans/2026-09-20-performance-hardening.md). Closes the last three items from V1's original review: `getHomeSummary`/`getStockDashboard` issuing one database query per SKU (and per SKU/warehouse pair) inside a loop, and `getMoneyDashboard`'s Daily COGS path re-reading a SKU/warehouse's entire ledger history and running two full FIFO passes once per day in the requested window.

## Method

Same subagent-driven-development process as every prior stream. Baseline before Task 1: 167/167 tests green. This is the fifth and final stream in the original post-V1 backlog's recommended order (B→A→E→C→D). The obvious first fix considered and rejected during design — wrapping each per-item loop in `Promise.all` — would have traded one bottleneck for a worse one: parallelizing thousands of concurrent queries at real SKU volume risks exhausting the mysql2 connection pool rather than actually helping. The design instead replaces each N-round-trips loop with a single grouped (`GROUP BY`) aggregate query, and replaces the Daily COGS path's per-day full recomputation with one query plus one chronological forward pass that accumulates each day's cost incrementally. Caching, precomputed tables, `IN`-list chunking, and query-count/timing tests were all explicitly considered and rejected as Non-Goals during design — the first three for introducing complexity or risk this system's actual scale doesn't need, the last because this codebase has no query-counting instrumentation and a wall-clock assertion against local loopback MySQL would be unreliable rather than a meaningful regression guard.

## Task-by-Task

**Task 1 — `getSohForSkus`.** A single grouped query (`GROUP BY skuId, warehouseId`) replacing what will become N per-SKU calls to `getSohByWarehouse`, added as a purely additive function alongside the old one. Reviewed clean, no fix round; both new tests confirmed non-vacuous, including the "a SKU with no ledger history is absent from the map, not present with an empty array" contract that later tasks depend on.

**Task 2 — `getDailyCogsForRange`.** One query (bounded to events on or before the end of the last requested date — events after the window can't affect FIFO consumption within it) plus one chronological forward pass, replacing what will become N per-day calls to `getDailyCogs`. The task reviewer didn't just check the code against the brief — it independently traced the general-case mathematical equivalence between the old "cost up to this day minus cost up to the day before" double-recomputation and the new single forward pass's per-day cost bucketing, verifying the split-batch-sale case, the zero-sale-day case, and the sale-at-start-of-history case, not just the one ported test scenario. Reviewed clean, no fix round.

**Task 3 — Rewire `dashboards.ts` (final planned task).** `getHomeSummary` and `getStockDashboard` now call `getSohForSkus` plus a new `getAverageDailySalesForSkus` helper once each before any per-SKU loop, with the loops themselves reduced to pure in-memory work; `getMoneyDashboard` calls `getDailyCogsForRange` once instead of `Promise.all`-ing per-day calls. `getSohByWarehouse` and `getDailyCogs` (and their direct tests) are deleted, confirmed via whole-repo grep to have zero remaining references. Every pre-existing test in `dashboards.test.ts` was diffed directly against its pre-refactor version — not just reviewed — to prove byte-for-byte no change, before two new multi-SKU tests were added proving the batched grouping/keying logic itself is correct. Reviewed clean, no fix round — the only task across this entire stream that needed zero fix rounds on every single task.

## Final Whole-Branch Review

One broad review pass, dispatched on the most capable available model, looked across all 3 tasks' combined diff for cross-cutting issues no single task-scoped reviewer could see. Rather than trusting any task's prior verdict, the reviewer independently re-derived the three claims most worth re-checking at the whole-diff level: read the current `dashboards.ts` in full and confirmed zero `await` expressions survive inside any loop in any of the three exported functions; verified the "a sale always has a matching ledger row" invariant `getStockDashboard`'s absent-key handling depends on by reading `recordSalesActual` directly and grepping to confirm it's the *only* writer into `sales_actuals` in the whole codebase, rather than trusting the design doc's own assertion; and read `enumerateDateStrings`'s actual implementation to confirm it genuinely produces ascending, UTC-only dates, which `getDailyCogsForRange`'s upper-bound logic depends on. The reviewer also gave an independent, plain-language query count for all three dashboards at a realistic 10,000-SKU scale — 6 flat queries for Home (down from roughly 40,000), 3 for Stock (same), and exactly 1 for Money's Daily COGS path regardless of window length (down from one query plus two full-history FIFO passes per day) — as the actual proof this stream achieves its stated goal, since the design deliberately excluded automated timing tests as unreliable.

Result: **0 Critical, 0 Important findings** — the first final review across all five streams in this engagement with no Critical or Important issues at all. Six Minor findings, none load-bearing: `getDailyCogsForRange`'s ascending-`dateKeys` precondition is documented in the plan but not in the function's own doc comment; the design's "identical failure behavior to the old approach" claim is very slightly overstated in the new code's favor (the new forward pass can surface a genuine insufficient-stock condition in a narrow case the old per-day early-return used to silently mask — judged a strict improvement, not a regression); the same caveat applies to exact floating-point equality (numerically equivalent, not literally bit-identical, for a long history); one ported test lost an implicit length constraint when it correctly became order-agnostic; `getShipmentLandedUnitCost` still has an await-in-a-loop bounded by shipment line count (not SKU count), now the last such pattern reachable from any dashboard; and the process-expected note that `BACKLOG.md`/`BUILD-HISTORY.md` get updated after this review lands, not before.

## Fix Wave + Residual

No fix wave — every finding across all three tasks and the final review was either clean on first pass or, at the final review, Minor and non-load-bearing. All 6 Minor findings from the final review are parked with rulings and tracked in `BACKLOG.md` section D's deferred-items list, following the exact same pattern established by every prior stream's own Minor findings, rather than spinning up a fix wave for documentation-precision and test-strength items on an otherwise fully-verified diff.

## Final State (Stream D)

- 3 tasks complete, each individually reviewed; zero fix rounds needed on any task — the cleanest single stream in this entire five-stream engagement by that measure. One whole-branch review found 0 Critical, 0 Important, and 6 Minor (parked) issues.
- Test suite: 171 tests, 23 files, passing on `pnpm test` (with `.env` sourced into the shell first — see the `pnpm test` environment-loading gap this review surfaced, tracked in `BACKLOG.md` section F). `pnpm check` (`tsc --noEmit`) passes clean.
- All three of V1's originally-flagged performance risks are closed: `getHomeSummary`/`getStockDashboard` issue a small constant number of queries regardless of active SKU count, and `getMoneyDashboard`'s Daily COGS path issues one query for an entire requested window instead of one-query-plus-two-FIFO-passes per day — both independently verified against the actual current code by the final reviewer, not assumed from the task-level reviews.
- Every dashboard's output was proven byte-identical to pre-refactor behavior by diffing pre-existing tests directly, not just re-reviewing them — the one place a subtle shape regression could plausibly have slipped through (a wider grouped-query row leaking an extra field into `getStockDashboard`'s output) was checked specifically and confirmed handled correctly.
- No known open Critical or Important defect anywhere in this stream. This closes the last item in the original post-V1 backlog's B→A→E→C→D recommended order — every stream from that backlog has now been built, reviewed, and closed out, save for one pre-existing gap tracked in section A (no UI to set a shipment's planned depart date) and each stream's own small deferred items in `BACKLOG.md`.

# Build History — Backlog Stream G: Inventory Ledger Completion & Navigation Restructuring

Built 2026-09-20, directly on `main` (same convention as every prior stream). Design spec: [`2026-09-20-inventory-ledger-and-navigation-design.md`](./2026-09-20-inventory-ledger-and-navigation-design.md). Plan: [`superpowers/plans/2026-09-20-inventory-ledger-and-navigation.md`](./superpowers/plans/2026-09-20-inventory-ledger-and-navigation.md). Not part of the original post-V1 backlog — added at the user's explicit request, after asking for the frontend navigation to mirror the 13-tab Google Sheets Control Tower structure the team already knows, which surfaced a far more serious latent gap during design: `recordLedgerEvent(eventType: "receipt")` was only ever called by the one-time migration script. Nothing in the live application's shipment-arrival flow wrote a receipt — after a real go-live, the app could never actually receive new stock, silently.

## Method

Same subagent-driven-development process as every prior stream. Baseline before Task 1: 178/178 tests green. Design work surfaced a second, structural gap while investigating the first: `shipments` had no `warehouseId` column anywhere, so there was no way to even say which warehouse a shipment's units land in — a prerequisite for writing a receipt at all (`inventory_ledger.warehouseId` is `NOT NULL`). Both gaps were folded into one plan rather than split, since the frontend batch-detail drill-down the user asked for is unbuildable without the backend fix underneath it. Fulfillment & DHL Actuals — a separate feature idea raised in the same conversation (modeling per-order fulfillment cost from Shopify data against DHL/3PL contract rates) — was deliberately scoped OUT to its own future stream, since real calibrated business logic for it already exists in `acc/tools/ff_invoice_audit/` (a separate Python codebase, validated against real invoices within ~4%) that a future spec must reuse rather than reinvent.

## Task-by-Task

**Task 1 — `warehouseId` on `shipments`.** Schema column, `createShipment`/router updates, and fixture fixes across every file that creates a shipment. The design spec's own blast-radius grep (`server/*.test.ts` and `scripts/*.test.ts` only) undercounted: it missed `scripts/reconcile-migration.ts`'s own `createShipment` call, and wrongly assumed two of the three named test files already had a warehouse fixture when only one did. The implementer caught both via `pnpm check` (the real completeness backstop, since `warehouseId` became a required field) and fixed them consistently with the codebase's existing conventions rather than working around the gap. Reviewed clean, no fix round.

**Task 2 — `markShipmentArrived` writes a real ledger receipt.** Rewritten to require costs already recorded (a deliberate "fail loudly, no silent EXW-only placeholder" ruling) and to atomically write one receipt per shipment line item inside a `db.transaction`, mirroring `recordSalesActual`'s established read-before-transact pattern. `logChange` gained an additive `dbClient` parameter so it could participate in the transaction. The implementer found the brief's assumed shape for the one pre-existing test using this function was wrong (no PO/line-item fixture, no status transitions) and adapted correctly, in the process surfacing a genuine latent `listChangeLog` same-second-tie ordering bug the new test flow made reachable. Reviewed clean, no fix round.

**Task 3 — `getRemainingBatches`.** New read function reconstructing current FIFO batch state from ledger history, generalizing (as a separate function, not shared code) the consumption loop `getDailyCogsForRange` already had, with wider event-type handling (adjustments). The plan's own third test fixture had a genuine arithmetic error — an expected value that summed to 110 across two batches when the total net quantity was fixed at 90 by the test's own events, impossible under any consumption order. The implementer caught it, corrected it, and the task reviewer independently re-derived the arithmetic from first principles and hand-traced the shipped algorithm against all 5 test cases before confirming the fix. Reviewed clean, no fix round.

**Task 4 — router additions.** `inventoryLedger.remainingBatches` and `payments.listTransactions`, both small and fully mechanical. Reviewed clean, no fix round.

**Tasks 5-7 — frontend.** Stock page FF/Mutual toggle plus a new Inventory Ledger batch-detail drill-down page (route-param only, same pattern as ChangeLog); a new standalone Transactions page extracted from Money's previously-buried matching UI; a new Catalog page (SKU/Vendor/Warehouse management against pre-existing backend procedures) and finalized navigation. All three reviewed clean on the diff itself. Task 7 additionally included genuine live manual verification against the real two-process dev server via a headless-Chrome CDP session (no browser automation library exists in this repo) — driving actual UI interactions, not just API calls — which surfaced a real, pre-existing, out-of-scope bug: `dashboards.money` 500s when unpaid payments in its query window span multiple currencies, already flagged in-code as an unresolved design question before this stream and confirmed via git history to be untouched by any of this stream's commits.

## Final Whole-Branch Review

Dispatched on the most capable available model, given the diff's size (~135KB across 7 commits) and the fact that the new receipt-writing logic is financially load-bearing — it determines real inventory valuation and COGS going forward. The reviewer hand-derived three FIFO/landed-cost scenarios independently rather than trusting any task's prior test run, including one case (a sale spanning two batches) not covered by any existing test as a distinct scenario, and specifically went hunting for escape hatches around the new "arrival requires costs recorded" business rule rather than only checking the rule's own code path.

That search found the review's Critical finding: `markShipmentArrived` had no guard against being called twice on the same shipment, and — because receipts are always positive-qty — `recordLedgerEvent`'s negative-stock guard could never catch a duplicate call. Nothing in this codebase has ever built a way to reverse a written ledger receipt, so a double-arrival (one click away, since the UI's arrival-date input prefills from the existing value with no "already arrived" disabled state) would have permanently corrupted every downstream SOH/FIFO/COGS calculation for that batch. Reviewing further up the same code path surfaced an Important finding that made the Critical one worse in spirit: `updateShipmentStatus` could still reach `"delivered"` directly, entirely bypassing `markShipmentArrived`, its cost guard, and the ledger write — the exact "stock never arrives" failure this whole stream was built to close, simply reachable through a different function.

Four more Important findings, each independently verified against the diff rather than assumed from a task report: duplicate-SKU shipment lines (two PO tranches for the same SKU, shipped together — a normal real-world case) silently collapsed to one landed cost, because the receipt-writing code keyed its per-line lookup by SKU instead of by line item; the Sheet-migration script didn't validate its new `warehouse` column, so a blank value would have created a warehouse with an empty code and name; the new Catalog page's SKU-create form offered `"barcode"` as an identifier type, which isn't a real value in the six-member enum and would have failed at the database layer; and `getRemainingBatches` had no secondary sort key for same-timestamp ledger events, plus a real (if currently dormant — no live code path writes `"adjustment"` events yet) ordering asymmetry against `recordLedgerEvent`'s end-of-day solvency guard.

## Fix Wave + Residual

One fix wave addressed all seven findings (six Important/Critical plus one bundled Minor — landed-cost rounding at 4 decimal places losing real money on a large receipt) as a single commit, per this process's "no second fix wave" rule. The Critical and its related Important finding were fixed together with one synthesis rather than two patches: `markShipmentArrived` now validates the shipment's current status against the existing transition table before proceeding (mirroring `markShipmentDeparted`'s own precedent exactly) and owns setting `status: "delivered"` itself, atomically, inside its own transaction — which closes the idempotency Critical as a direct consequence, since `delivered → delivered` was already defined as an invalid transition, not as a second bolted-on check. `updateShipmentStatus` now rejects `"delivered"` the same way it already rejected `"departed"`, pointing the caller at the right function. The scoped re-review verified all seven findings addressed with file:line evidence and found no new Critical or Important breakage, but did catch two things worth recording precisely: the fix wave's own report mis-attributed a `MoneyPage.tsx` duplicate-React-key issue as a new side effect of the duplicate-SKU fix, when re-reading the pre-fix code showed it was already there (the fix only added a field, not a new array entry) — corrected in the ledger rather than left as stated; and the fix itself introduced one new Minor of its own, a client-side import of `SKU_IDENTIFIER_TYPES` that pulls the entire Drizzle schema module into the browser bundle, flagged for a follow-up rather than reopening the round for a non-load-bearing bundle-size concern.

## Final State (Stream G)

- 7 tasks complete, each individually reviewed; zero fix rounds needed on any individual task — every task-level defect found across this stream was either a plan-authored error the implementer caught and corrected (Tasks 1 and 3) or absent entirely (Tasks 2, 4-7). One whole-branch review found 1 Critical + 5 Important + 1 Minor, all closed in a single fix wave; the scoped re-review confirmed all seven addressed and found one new Minor plus one pre-existing issue the fix report had mis-attributed as new, both corrected in the record and parked as deferred, non-load-bearing items.
- Test suite: 192 tests, 23 files, passing on `pnpm test` (with `.env` sourced first, per the still-open Section F gap). `pnpm check` (`tsc --noEmit`) passes clean.
- The load-bearing gap this stream exists to close is now real and tested: a shipment marked arrived writes one atomically-committed ledger receipt per line item, at its real per-line landed cost, gated on costs already being recorded, guarded against being written twice, and no longer reachable to bypass via any other status-transition function.
- Five items surfaced during this stream are deliberately deferred, tracked in `BACKLOG.md` section G — most notably a genuine, independently-reproduced, pre-existing multi-currency cashflow 500 on the Cost & Cashflow page (confirmed via git history to predate and be untouched by this stream) that needs its own design decision, and the fact that no shipment-create UI exists anywhere in the client today, so the receipt-writing path this stream built has no way to be reached end-to-end from the app itself yet.
- No known open Critical or Important defect. This is the first stream in this engagement added outside the original post-V1 backlog's recommended order, initiated directly from a user conversation about frontend usability that surfaced a correctness gap more serious than the UI question that prompted it.

# Build History — Backlog Stream H: Weekly Sales Plan Input & Generation

Built 2026-09-20, directly on `main` (same convention as every prior stream). Design spec: [`2026-09-20-weekly-sales-plan-design.md`](./2026-09-20-weekly-sales-plan-design.md). Plan: [`superpowers/plans/2026-09-20-weekly-sales-plan.md`](./superpowers/plans/2026-09-20-weekly-sales-plan.md). Added at the user's request, immediately after Stream G — a follow-up conversation asking for two more manual-entry UI gaps (shipment and purchase-order creation forms) surfaced a request for a genuinely new planning capability: enter a week's revenue, a per-SKU units-per-€1,000 mix, and a warehouse-split percentage, and have the system compute the day-by-day, per-SKU, per-warehouse breakdown, up to 26 weeks ahead. Investigating how to store that safely surfaced a second, unrelated, already-live bug: `sales_plan` had no uniqueness on (SKU, warehouse, day) at all, so the existing one-entry-at-a-time manual form could already silently duplicate a day's plan.

## Method

Same subagent-driven-development process as every prior stream. Baseline before Task 1: 194/194 tests green. Design brainstorming ran conversationally rather than through the visual companion — three rounds of the user correcting and sharpening the mechanics (a 13-26 week rolling horizon rather than one week at a time, flat revenue/7 rather than a day-of-week pattern, and a request to look at how the real Google Sheets Control Tower already separates actual/planned/deviation) directly shaped the final design, including surfacing the exact schema fix (a real unique constraint plus an upsert) that made the whole feature safe to build on.

## Task-by-Task

**Task 1 — Schema.** Unique constraint on `sales_plan`, upsert `createSalesPlanEntry`, two new tables (`sales_plan_weekly_inputs`, `sales_plan_weekly_recipe_lines`). The plan's verbatim FK declaration would have auto-generated a 75-character MySQL identifier, exceeding the 64-char limit — caught by `pnpm db:push` itself, fixed with an explicit shorter constraint name, verified by the task reviewer to change nothing else about the FK. Reviewed clean, no fix round.

**Task 2 — Core generation logic.** `regenerateSalesPlanForWeek` and `upsertWeeklyInput`, the heart of the feature. This task is the most consequential in the stream: the implementer found and fixed a genuine gap in the *plan's own specified code*, not an implementation mistake — a recipe change alone left removed SKUs' `sales_plan` rows orphaned forever, since `regenerateSalesPlanForWeek`'s delete step only ever sees the recipe *after* it had already been replaced. Fixed at the composition level (`upsertWeeklyInput` captures the previous recipe before replacing it), verified by the task reviewer via hand-tracing the exact scenario the design's own test was written to catch. The task reviewer's own re-review then surfaced a second, closely related gap the first fix didn't cover: a SKU that *stays* in the recipe, but whose warehouse pair gets reassigned on the same save, also leaves stale rows under the old pair — the same invariant violation via a different trigger, and one that becomes completely routine the moment Task 4's UI ships (every week's warehouse selects are editable on every save). The controller judged this load-bearing rather than deferring it, and a second fix round unified both cases into one simpler rule: unconditionally clean up every SKU from the *previous* recipe under the *previous* warehouse pair, before every regeneration — removing an entire class of future bugs rather than closing two specific instances of it. Two fix rounds, both re-reviews clean.

**Task 3 — Router.** Two new tRPC procedures, thin wrappers over Task 2's already-tested functions. Reviewed clean, no fix round.

**Task 4 — Frontend.** A 26-week rolling grid on the Stock page — each week independently editable, current week included, past weeks read-only. Reviewed clean on the code itself; the task reviewer flagged that the implementer's own live-dev-server verification claim was narration rather than pasted command output — not a code defect (the reviewer's own independent hand-traces of the Monday-computation and past-week-boundary logic already held up), but an evidentiary gap. Rather than spending a fix-round cycle on a report-quality issue with no code to change, the controller personally re-ran the exact live verification directly against the real dev server: seeded fresh fixtures, called the real endpoints with the exact payload the UI issues, and read the resulting `sales_plan` rows from the database directly — 14 rows, a correct 35/15 warehouse split for a real €70,000/week, 5-units-per-€1,000, 70/30 scenario, independently confirming what the implementer claimed.

## Final Whole-Branch Review

Dispatched on the most capable available model, explicitly instructed not to trust that two fix rounds meant Task 2's cleanup logic was exhausted — to construct fresh scenarios beyond what either task-level re-review had already checked. It did: three new scenarios (three consecutive saves with different recipe/warehouse combinations each time; a partial warehouse-pair overlap; date-range scoping against an adjacent week) all traced correct, and the review additionally confirmed by direct grep that `regenerateSalesPlanForWeek` still had zero `.transaction()` calls after both rounds of surrounding edits, and that a manually-entered plan for an unrelated SKU could never be reached by either the bulk-generation or cleanup delete regardless of shared date.

Having confirmed the core logic solid, the review found everything wrong sat at the edges no per-task review had a vantage point on: **zero numeric validation** anywhere on revenue, warehouse-split percentage, or the recipe ratio — a percent above 100 silently wrote a *negative* planned quantity, since nothing but application logic stood between a bad string and an `int NOT NULL` column; **a test suite with a 22-day expiration date** — the whole `regenerateSalesPlanForWeek`/`upsertWeeklyInput` test block hardcoded a week that would itself become "in the past" and start failing its own guard within a month, caught only because the reviewer thought to check whether a passing suite today would still pass in three weeks; **a migration with no de-duplication step**, adding a uniqueness constraint against a table the design's own spec states already has live duplicates — exactly the condition this whole stream exists to fix, which would have made the migration fail on the first real deployment that needed it; and **a new circular import** between `salesPlan.ts` and `dashboards.ts`, working today purely by accident of JavaScript's function-hoisting rules. Two smaller, previously-deferred items were promoted to must-fix: the one test covering the rounding rule's warehouse split only asserted a sum, which would pass even under a swapped or independently-rounded split; and nothing stopped a user picking the same warehouse twice or repeating a SKU in one week's recipe, which would surface as a raw database error instead of a clear one.

## Fix Wave + Residual

One fix wave closed all six findings as a single commit. The migration fix was verified beyond just reading it correct: since the dev database already has the constraint applied and can't hold live duplicates to test against, the implementer built a throwaway table with the same shape, inserted genuine duplicate rows, ran the exact `DELETE` statement being added to the migration, confirmed the highest-id row survived exactly as intended, then dropped the table — real verification of a fix that couldn't be exercised through the normal test suite. The scoped re-review independently retraced all six findings against the actual diff (not the report's narrative) and found no new breakage; one Minor item — the new Zod `.refine()` guards have no dedicated regression test — was parked as deferred rather than extending the round, since neither finding demanded one and the logic was independently confirmed correct by direct reading.

## Final State (Stream H)

- 4 tasks complete; Task 2 required two fix rounds, both closing genuine design-level gaps found during the stream's own build rather than implementation mistakes, both re-reviews clean. One whole-branch review found 4 Important + 2 promoted-from-deferred issues, all closed in a single fix wave with independently-verified evidence (including live isolation-testing of a migration statement that couldn't be exercised any other way), confirmed clean by a scoped re-review.
- Test suite: 209 tests, 23 files, passing on `pnpm test`. `pnpm check` (`tsc --noEmit`) and `pnpm build` pass clean.
- The load-bearing gap this stream exists to close is now real and tested: `sales_plan` cannot hold a duplicate row (structurally, via a real unique constraint, with the one live-data migration path verified safe against pre-existing duplicates), and a week's sales plan can be entered once as revenue+recipe+split and generated correctly across 7 days, any number of SKUs, and both warehouses — replacing, never duplicating, whatever existed before for that exact combination, regardless of how many times or in what order the recipe or warehouse pair is subsequently changed.
- One item surfaced during this stream is deliberately deferred, tracked in `BACKLOG.md` section H: a small (≤1 unit/SKU/week), by-design, previously-undocumented rounding drift between daily-rounded planned quantities and the mathematically exact weekly total.
- No known open Critical or Important defect. Second stream in this engagement added outside the original post-V1 backlog's recommended order, and the second in a row where a design-level gap was found and fixed within the stream's own build (Task 2's two fix rounds) rather than shipped and discovered later — matching the discipline already established in Streams B/A/E/C.

# Build History — Backlog Stream I: Three-Lens Architecture Review

Built 2026-09-20, directly on `main`. Design doc: [`2026-09-20-three-lens-architecture-review.md`](./2026-09-20-three-lens-architecture-review.md). Not a feature stream — a full re-review of the whole codebase and product at the user's explicit request, using the three-critique-lens ritual this engagement has used for individual decisions since its start ("Кук, Джобс, Борис Черний"), applied for the first time to the entire system rather than one design choice.

## Method

Three independent fresh reviews, dispatched with no context on each other or on this engagement's own prior self-assessment, each on the most capable available model, each explicitly told to explore the repo cold and verify every claim against real code rather than trust docs: an operational-excellence lens (would this actually hold up in real day-to-day supply-chain operation — the Tim Cook read), a product-coherence lens (is this one considered product or features from different eras bolted together — the Steve Jobs read), and an engineering-rigor lens (is the type safety and architecture real or theater — the Boris Cherny read). All three came back longer and sharper than any single-stream review in this engagement's history, because for the first time the review subject was 8 streams' accumulated interactions with each other, not one diff.

The three reports converged independently on several of the same findings from different angles — most strikingly, the shipment `weightShare`/`valueShare` NaN-corruption path was flagged by all three lenses without any cross-contamination between the dispatches. That convergence, plus anything already actively corrupting data or crashing a dashboard, became the immediate fix list; everything else — a genuinely large volume of real findings, including several the reviewers ranked as the most important structural work ahead of this codebase — was triaged into `BACKLOG.md` section I rather than attempted in the same pass, since several of those (unifying three FIFO implementations, migrating every money column from `varchar` to `decimal`, a schema-wide not-found-handling convention) are properly-scoped future streams in their own right, not patches.

## What Was Fixed

Six corrections, each verified against the actual diff by a fresh reviewer, with a full fix-round + scoped re-review cycle before commit — the same discipline as every SDD-built stream, applied here to a directly-implemented batch rather than subagent-dispatched tasks:

1. **`getShipmentLandedUnitCost` validates `weightShare`/`valueShare`** — finite, in [0,1], and summing to 1 across a shipment's lines, closing the single most severe finding in the whole review (independently flagged by all three lenses): a bad or defaulted share previously flowed unchecked into a permanently-written ledger cost with no reversal path anywhere in this codebase.
2. **`getDailyCogsForRange` now handles `"adjustment"` ledger events**, closing a real, live divergence between it and the parallel `getRemainingBatches` function — the engineering-lens review's own top-ranked structural finding, verified by hand-tracing three fresh scenarios (not just the ones the fix's own tests covered) during the final review. The fix round itself needed one more pass: the first attempt introduced a same-timestamp query-ordering hazard the sibling function didn't have, left the new failure mode unguarded in the one dashboard that calls it, and shipped a test that would have passed without the fix — all three caught by a fresh re-review before commit, not discovered later.
3. **Removed the Stock page's manual "Add plan entry" form**, which the product-lens review found silently destroying data written by the Weekly Sales Plan generator shipped one stream earlier — two live writers to the same table, on the same page, with no coordination between them and no warning in either direction.
4. **Currency codes normalized to uppercase** before comparison in `getCashflowForecast` — a `"eur"` vs `"EUR"` typo previously crashed both Home and Money outright.
5. **`createSku` genuinely supports all 6 identifier types** instead of an `as any` cast hiding a schema that only worked for 2 of the 6 options the UI actually offered — closing a guaranteed-to-fail path the engineering-lens review demonstrated by simply removing the cast and re-running `tsc` clean.
6. **Deleted a dead, misleading duplicate `AppRouter` type export** that shadowed the real one and would silently strip a future import's type safety.

## Final Review

A fresh reviewer, given the exact same rigor as any final whole-branch review in this engagement, caught real problems in the first fix pass rather than approving on the strength of the six individual explanations: the FIFO-adjustment fix's query didn't pin same-timestamp ordering the way its sibling function did (a new hazard the fix itself introduced, not inherited); the dashboard call site the fix flows into had no error boundary, unlike its sibling landed-cost call a few lines below it; the fix's own regression test used numbers that happened to produce the same result whether or not the fix was applied, so it protected nothing; and the SKU-identifier fix closed the hole for 4 of 6 identifier types while leaving the same NOT-NULL failure open for the other 2 (`sku` and `name`) inside the very form field it rewrote. All four were fixed in one round and confirmed by hand-derived arithmetic and independent code tracing in the scoped re-review, not by re-running the test suite and trusting a green result.

## Final State (Stream I)

- 6 corrections applied directly (not via subagent dispatch, matching this engagement's precedent for small bounded fixes), each through a full review → fix-round → scoped-re-review cycle before commit. Test suite: 214 tests, 23 files, up from 209. `pnpm check` and `pnpm build` pass clean.
- The full three-lens review is preserved verbatim in its own doc for future reference — roughly 40 distinct findings across operations, product, and engineering, of which 6 (the most severe and most cross-confirmed) are closed and the rest are tracked in `BACKLOG.md` section I, organized by lens, for future prioritization.
- This is the first review in this engagement to examine the whole system's accumulated cross-stream interactions rather than one stream's own diff — and it found exactly the class of bug that kind of review exists to catch: two features from different streams silently fighting over the same data, and three independently-written implementations of the same algorithm quietly disagreeing on an edge case none of their individual test suites exercised.

# Build History — Operational (Cook) Cleanup

Built 2026-09-20/21, directly on `main`, immediately after Stream I. The user set an explicit priority order across Stream I's tracked-but-not-yet-built list — Operational first at full commitment, then Engineering, then Product — and this closed every bounded item Cook's lens had flagged.

Eight bounded fixes, each verified with a real test and a full `pnpm test`/`pnpm check` pass before commit (5 commits: `c8f99a3`, `47ccf50`, `302191c`, `0d4c99d`, `e4b180d`):

1. Production deploys must use `pnpm db:migrate`, never `pnpm db:push` — the latter authors a new migration from whatever `schema.ts` looks like at deploy time instead of only applying already-committed ones. Added the script, rewrote `RAILWAY.md`'s deploy step with the risk spelled out.
2. `nightlyExport.ts`'s hand-maintained `CORE_TABLES` list had drifted from the schema, silently dropping Stream H's 2 sales-plan-input tables from every nightly export since they shipped.
3. Matching a transaction to a payment never actually marked the payment paid — `matchTransactionToPayment` only ever set `matchedPaymentId`, so `payments.paid` and "is this payment matched" silently diverged forever. Fixed by extracting `markPaymentPaidCore` from `markPaymentPaid` (the same core/wrapper transaction pattern Stream H established) and having matching call it atomically, using the transaction's own amount/fxRate/date — without overwriting an already-paid payment's recorded values.
4. Real UI added for two mutations that already existed on the backend with zero UI callers: advancing a Purchase Order's status (`AdvanceStatusControl`, walking the existing linear `VALID_TRANSITIONS` chain), and recording a bank transaction (`RecordTransactionForm` on the Transactions page).
5. Overdue payables were invisible from Home — `nearTermCashNeeds` only ever looked at the forward 14-day window, so an unpaid payment already past its expected date fell outside every window Home computed. Added a second `getCashflowForecast` call over `[epoch, yesterday]`, reusing the existing mixed-currency estimate/flag logic rather than re-summing amounts by hand.
6. A SKU with zero ledger history (never received, never sold) was structurally excluded from the stockout-risk count — an empty `byWarehouse` array meant the risk loop's body never ran, so it always counted as safe. Exactly backwards: no stock and no demand signal is the highest-risk state.
7. `MoneyPage`'s Daily COGS/Landed Cost tabs silently scoped to whichever SKU/warehouse/shipment happened to be first in the list — a V1 placeholder the code's own comment admitted. Replaced with real dropdowns per tab and a plain prompt when nothing is selected.

Test suite: 218 tests (up from 214), `pnpm check` clean throughout. Deliberately deferred to a future session (need a real design pass or external infra decisions, not bounded fixes): reversal/correction paths for stock/cost/payment data, freight/duty lock-after-arrival, the nightly export's unbounded-query OOM risk, Shopify pull scheduling/alerting, and monitoring/restore-drill infrastructure — all tracked in `BACKLOG.md` section I.

# Build History — Backlog Stream J: Engineering Hardening

Built 2026-09-21, directly on `main`, via subagent-driven-development (the first of the three post-Stream-I priority tiers to use full SDD task dispatch rather than direct fixes — Operational above was small enough to fix directly; this tier's 7 tasks each got a fresh implementer and a fresh reviewer). Design doc: [`2026-09-21-engineering-hardening-design.md`](./2026-09-21-engineering-hardening-design.md). Plan: [`docs/superpowers/plans/2026-09-21-engineering-hardening.md`](./superpowers/plans/2026-09-21-engineering-hardening.md).

## What Was Built

Seven tasks addressing every bounded Engineering (Cherny) finding from Stream I's tracked list:

1. **Unified the two live FIFO implementations.** `getRemainingBatches` (`inventoryLedger.ts`) and `getDailyCogsForRange` (`salesPlan.ts`) had already diverged once (Stream I fixed their disagreement on adjustment handling) — both now call one shared `replayLedgerEventsFifo`, proven behavior-identical by every existing test for both functions passing unchanged.
2. **Deleted dead code** — `computeFifoCogs` (a third, already-unused FIFO implementation, superseded by task 1) and its now-fully-dead `LandedBatch`/`SaleEvent`/`FifoCogsResult` types, plus `updateSku` (zero callers).
3–4. **Made 8 mutation functions atomic with their audit log**, across `purchaseOrders.ts` (2 functions) and `shipments.ts` (6 functions) — each previously updated a row and called `logChange` as separate, unguarded statements (a crash between them left the change unaudited), and 4 of the 6 `shipments.ts` functions used the selected row unguarded, crashing with a raw `TypeError` instead of a clear error on a nonexistent id, while 3 sibling functions in the same file already did this correctly. All 9 now match.
5. **Added 9 missing foreign keys and 2 composite indexes.** Hit a real, well-evidenced blocker mid-build: applying the new FKs to the real dev DB made the *test suite* fail 71/221 tests, because 7 test files (later found to be 8) hardcoded `createdBy: 1`/`changedBy: 1` with no `users` row ever seeded, and 2 dev-DB rows were genuinely orphaned. This was the exact scenario the plan's own text anticipated ("a test fixture needs fixing... do not weaken the constraint") — not a real stop condition. Resolved with a controller ruling: added a `createUser` test helper matching the existing `createSku`/`createVendor`/`createWarehouse` pattern, fixed all 8 affected test files plus a real production-code instance of the same gap in the one-time migration script (via a transactionally-safe get-or-create system user), and cleared the 2 confirmed-orphaned rows on this local, non-production dev database.
6. **Migrated ~15 money/fractional-share columns from `varchar` to `decimal`** — the highest-risk task, since it touches money precision. Exact precision chosen per column and proven with a round-trip test that passes both before and after the migration. Surfaced two real, correctly-handled collateral issues: MySQL's decimal zero-padding (`"0.5"` → `"0.500000"`) required updating several pre-existing tests' literal assertions to match (numeric value unchanged, only string representation), and a latent FK-cleanup-order bug in 3 unrelated test files that task 5's new FKs had exposed but that only surfaced once this task's full-suite run happened to trigger it.
7. **Renamed the 5 `.mjs` CLI scripts to `.ts`**, closing a real blind spot: `tsconfig.json` has no `allowJs`, so these files were invisible to `tsc` entirely (confirmed: zero errors, zero mentions, on a clean `pnpm check`). The rename alone — no logic change needed, since all 5 already import their real logic from typed `.ts` core modules — surfaced and fixed 3 genuine latent type errors: unguarded `err.message` access on `unknown` in three catch blocks, and an implicit-`any` parameter pair.

Every task went through fresh-implementer → task-reviewer (spec + quality) → fix round(s) where needed → scoped re-review, per this project's established SDD process. Two commit-trailer mistakes (an implementer subagent's report using its own model name instead of this repo's uniform attribution convention) were caught and fixed within the first two tasks; every subsequent task's reviewer was told to independently verify the trailer via `git log` rather than trust the diff package, which cannot show commit metadata.

## Final Whole-Branch Review

Dispatched on the most capable available model, reviewing the full 8-commit range in one pass. Found 3 Important + 9 Minor issues — the exact class of cross-task interaction no single task's own review could see, since each task only reviewed its own diff:

- The Task 6 decimal migration's rewrite of a `landedCost.test.ts` test (replacing an app-level validation check with a DB-level rejection check, since the migration made the non-numeric half of the original guard unreachable at the app level) left the guard's separate range check ([0,1] bounds) with zero test coverage anywhere, and used a message-less `.rejects.toThrow()` that would pass on any thrown error.
- `change_log` audit rows for decimal-typed fields recorded `oldValue` (read back from the DB, now zero-padded) and `newValue` (the raw caller string) in two different representations — a real audit-trail bug introduced by Task 6 landing on top of Tasks 3/4's `logChange` calls, invisible to either task's own review since neither could see what the other would later do to the same code.
- A `payments.test.ts` assertion passed by numeric coincidence rather than by matching the actual production formula.

One fix-wave dispatch addressed all 3 Important findings plus 6 selected Minor ones (a non-round-tripping test value, an inconsistent test-fixture pattern, an over-permissive system-user role, a missing migration pre-flight-check doc note, and a missing atomicity test) — 3 Minor findings were deliberately left to the backlog as genuinely pre-existing or out of scope (a `markShipmentArrived` TOCTOU wart that predates this stream, router-layer numeric input validation the design doc explicitly deferred, and one unreferenced ownership column outside the stream's original list). A scoped re-review confirmed all 8 addressed with no new breakage.

## Final State (Stream J)

- Test suite: 227 tests, 24 files (up from 218 at the end of the Operational cleanup: −3 dead-code tests, +2 PO atomicity, +4 shipment atomicity, +3 decimal round-trip, +3 final-review fix-wave). `pnpm check` clean throughout.
- Every bounded Engineering (Cherny) item from Stream I's tracked list is now closed; the design doc's explicitly-deferred items (router-layer input validation, `markShipmentArrived`'s read-outside-transaction pattern, `nightlyExport.ts`'s hand-maintained table list) remain tracked in `BACKLOG.md`.
- Next per the user's explicit priority order: Product (Jobs)-lens items.

# Build History — Backlog Stream K: Product Coherence

Built 2026-09-21, directly on `main`, via subagent-driven-development. Third and final tier of the priority order set after Stream I (Operational → Engineering → Product). Design doc: [`2026-09-21-product-coherence-design.md`](./2026-09-21-product-coherence-design.md). Plan: [`docs/superpowers/plans/2026-09-21-product-coherence.md`](./superpowers/plans/2026-09-21-product-coherence.md).

## What Was Built

Six tasks addressing every bounded Product (Jobs) finding from Stream I's tracked list:

1. **Extracted shared `skuLabel`/`warehouseLabel` helpers** into a new `client/src/lib/labels.ts`, deduplicating logic `StockPage.tsx` and `MoneyPage.tsx` had each independently reinvented, and applied them everywhere a raw numeric id was still shown (`ShipmentsPage.tsx`, `PurchaseOrdersPage.tsx`, `MoneyPage.tsx`'s Landed Cost tab, and a full rewrite of `InventoryLedgerPage.tsx`, which previously showed zero real names anywhere).
2. **Added `formatMoney`** (currency symbols for EUR/USD/CNY/GBP, falling back to the uppercased code) and standardized 4 raw `Date.toString()` displays to this codebase's dominant `.toISOString().slice(0, 10)` convention.
3. **Made the Change Log reachable** (links from Purchase Orders and Shipments to the shared `/change-log/:entityType/:entityId` route, which had zero links anywhere despite working correctly) and fixed a second, previously-unnamed instance of a "guaranteed-to-fail control" bug class: `StatusTransitionControl` offered "advance to delivered" for a `customs`-status shipment, which the backend unconditionally rejects (a dedicated control already handles that transition).
4. **Replaced the fixed 21/45/90-day stockout thresholds** with a per-SKU reorder point — `skus.leadTimeDays`/`safetyStockDays`, defaulting to Jello's real known 66/14-day replenishment cycle, editable per SKU.
5. **Made Home's 4 static counters clickable**, linking each to the page where its underlying data can actually be worked.
6. **Revived `updateSku`** (deleted as dead code by the prior Engineering stream, now with real callers) scoped narrowly to `status`/`leadTimeDays`/`safetyStockDays` — identifier fields stay create-only, since editing a live identifier has downstream implications out of scope for this task. Added `updateVendor`/`updateWarehouse` for edit. Vendor/Warehouse *archiving* deliberately deferred: neither table has a status column today, and what "archived" should mean for an entity with existing POs/shipments needs its own design pass.

Every task went through fresh-implementer → task-reviewer → fix round(s) where needed → scoped re-review. Task reviewers independently verified nontrivial claims rather than trusting reports at face value: one hand-traced the exact SOH/avgDailySales/daysOfCover arithmetic in two new tests against the real ledger mechanics (not just checking the test passed); another checked Drizzle's actual `mapUpdateSet` source to correct an implementer's own speculation about a "silent no-op" (it actually throws `"No values to set"`); a third traced all 5 real shipment statuses through a control-visibility fix to confirm every guaranteed-to-fail path was genuinely eliminated, not just the two named in the brief.

## Final Whole-Branch Review

Dispatched on the most capable available model. Found 2 Important + 8 Minor issues — again, exactly the class of cross-task interaction no single task's own review could see:

- **The `CustomsArrivalControl` fix from Task 3 was incomplete.** It correctly gated the arrival button on `shipment.status === "customs"`, but missed that `markShipmentArrived` has a SECOND, independent precondition (freight/duty costs must already be recorded) — so a shipment in the ordinary real-world state of "in customs, invoice not yet in" still showed a fully-enabled, guaranteed-to-fail button. The reviewer found this by reading the actual backend guard clause the earlier task's own reviewer hadn't cross-checked against.
- **Task 2's date-format standardization broke the audit log.** `ChangeLogPage.tsx`'s `changedAt` is a real `timestamp`, not a date-only concept like the three payment/PO fields the same task correctly standardized — truncating it to a bare day destroyed same-day multi-edit ordering, the one place in the whole stream where "cleaning up" a format actually removed real information.

One fix-wave dispatch addressed both Important findings plus 6 selected Minor ones (a dead half of a filter resting on a false premise about which shipment statuses were actually reachable, wrong hint copy for a terminal shipment status, three Catalog table column-count mismatches, a silent-zero-on-clear input gap, missing non-empty guards on the newly-added Warehouse/Vendor edit forms, and one remaining bare-currency display the earlier currency task's simple `{amount} {currency}` pattern-match had missed). A scoped re-review confirmed all 8 addressed with no new breakage.

## Final State (Stream K)

- Test suite: 232 tests, 24 files (up from 227 at the end of Stream J: +2 reorder-point tests, +3 catalog-update tests). `pnpm check` clean throughout.
- Every bounded Product (Jobs) item from Stream I's tracked list is now closed. Deferred (need their own design pass or real-data validation, not bounded fixes): Vendor/Warehouse archiving; recalibrating the 66/14-day reorder-point defaults against real Jello data once available (flagged by the final review as likely to read most of a normal ~60-90-day-cover catalog as at-risk); no audit trail on the 3 new catalog `update*` functions (consistent with this app's existing PO/shipment/payment-only audit scope).
- This closes out the full three-lens follow-through the user ordered after Stream I: Operational (2026-09-20/21) → Engineering (Stream J, 2026-09-21) → Product (Stream K, 2026-09-21), each tier fully closed before the next began.

# Build History — Backlog Stream L: Control Tower Live Integration

Built 2026-09-21, directly on `main`, via subagent-driven-development (10 tasks plus 4 controller-directed fix commits outside the original numbering). Design doc: [`2026-09-21-control-tower-live-integration-design.md`](./2026-09-21-control-tower-live-integration-design.md). Plan: [`docs/superpowers/plans/2026-09-21-control-tower-live-integration.md`](./superpowers/plans/2026-09-21-control-tower-live-integration.md). Ledger: `.superpowers/sdd/2026-09-21-control-tower-live-integration/progress.md`.

This stream ports a previously-validated real-data migration/reconciliation tool — built and proven on a separate, unmerged branch (`feat/real-data-dry-run`, real-data dry-run passed 2026-09-19, 1,209 checks, 0 mismatches) — onto current `main`, which had diverged by 118 commits (Backlog Streams B through K) since that branch forked. The port is not a mechanical copy: the design doc identified 9 real adaptation points where `main`'s own evolution (the FIFO unification, decimal-column migration, `markPaymentPaidCore` extraction, `dbClient`-threading conventions, the migration-system-user pattern, `lineItemId`-aware landed cost) changed the shape of what the ported code needed to call, in some cases making the port's own original design *stale in main's favor* (e.g. `markPaymentPaid`/`matchTransactionToPayment` already gained the `dbClient` support the original design asked for, via a different, better mechanism than the design assumed).

## What Was Built

1. **Pure port** (`csv.ts`, `gviz.ts`, `snapshot.ts`, real Sheet fixtures captured 2026-09-19) — zero adaptation, byte-identical, independently confirmed by diff against the source branch.
2. **Backorder policy** — `ALLOW_BACKORDERS_SETTING` gating `recordLedgerEvent`'s negative-stock guard.
3. **Backorder-aware Daily COGS series** — `computeFifoDailySeries` in `server/landedCost.ts`, with fresh local input types (not a resurrection of the dead types Stream J deleted); `getShipmentLandedUnitCost` gained a `dbClient` parameter and `lineItemId`-aware handling.
4. **`recordSalesActual` gained an optional `dbClient` parameter**, joining an existing transaction instead of nesting one.
5. **`migrate-from-sheet.ts` extended**: paid flags/dates on payments, planned/actual dates on shipments, pooled-container merging (`<prefix>Container<N>-<SKU>` rows collapse into one shipment), `matchedRef` on transactions, new sales-actuals/sales-plan transforms. Two real architectural gaps in the plan's own text were found and ruled on mid-task rather than blindly followed: pooled-container merging was assigned to the wrong file in the original plan (ratified as implemented, in `migrate-from-sheet.ts`, not re-done to match the source branch's file layout); shipment-owned payments (a real, validated Sheet mapping rule) needed a nullable `poNumber`/`shipmentRef` on `TransformedPayment`, correctly deferred to Task 6.
6. **`reconcile-migration.ts` extended** — paid-flag handling, transaction-to-payment matching, sales-actuals/sales-plan writes, pooled-container payment summing, the 1%-then-5% variance-tier link-transfer rule, and real landed-cost reconciliation wired to a live data source for the first time (previously `runMigration` threw loudly rather than accept `landedCostTotals`, since the real Sheet's column names were unknown before this stream).
7. **`reconcile.ts`/`targets.ts`/`export.ts`/`report.ts`/`classifications.ts`** — the R1–R7 reconciliation-target readers and markdown report, ported with `export.ts` adapted (per the Task 5 ruling above) to emit raw ungrouped rows rather than duplicate the pooling logic `migrate-from-sheet.ts` now owns.
8. **Orchestrator, CLI, ops**: `scripts/run-dryrun.ts`/`scripts/parallel-run.ts` (born `.ts`, not `.mjs`, matching Stream J's convention), `ops/` launchd files, `package.json` scripts.
9. **Decimal round-trip test** (`server/controlTowerDecimalMigration.test.ts`) driving a real migration input through `runMigration` at the columns' real, then-current 8dp precision.
10. **Re-ran the dry-run against the live Control Tower Sheet** and closed out this document — see "Task 10: the real re-run" below.

## Task 6: the `markPaymentPaidCore`-vs-`matchTransactionToPayment` judgment call

The one genuine design judgment call this plan delegated to the implementer rather than fully specifying. **Decision actually made**: the migration bypasses `matchTransactionToPayment`/`matchTransactionToPaymentCore` entirely — it writes `matchedPaymentId`/paid-flag fields directly onto payment/transaction rows inside its own transaction, rather than calling the live app's own matching function from inside the migration.

This was independently verified correct by an opus-tier review (escalated above the stream's default sonnet reviewer tier given the stakes — real Jello financial data). The review found the bypass is actually **more necessary** than the implementer's own original reasoning claimed: on current `main`, `matchTransactionToPayment` takes no `dbClient` parameter at all, so calling it from inside the migration's transaction would use a different, separately-committed pooled connection and silently escape the transaction/rollback boundary — regardless of the instalment-matching-invariant conflict (many transactions can settle one pooled payment; the live app's own matching function assumes one-to-one) that was the implementer's originally stated reason. The live app's own matching invariant is untouched (only a 1-line `export` was added to `markPaymentPaidCore` so the migration could call it directly, inside its own transaction, for the one-time bulk write).

Task 6 was also the largest and most rigorously reviewed task in the whole engagement on its own terms: 3 full fix-and-re-review cycles (every round escalated to opus given the stakes), closing 1 genuine Critical (pooled payment-slot summing grouped rows *after* per-row validation, so a group with one invalid sibling row could silently commit a payment for a fraction of the container's real amount) and 6 Important findings — two of which were regressions introduced by the fix rounds' own diffs (a duplicate-row-summing variant of the same Critical; a lone pooled payment silently dropped as "unresolved owner"). Round 3's reviewer pushed deliberately hard with adversarial cases beyond the implementer's own tests and gave a structural argument for why the fix could not regress further; it held.

## The precision-bug chain (between Task 8 and Task 9, outside the original task numbering)

Task 8's own real-DB fixture-replay test (`dryrun.test.ts`) failed on first run — R4 (landed cost) rejected 10 SKU lines by `6e-6`–`4e-5` at the `1e-6` tolerance, rolling the migration back. This did not get accepted at face value (the implementer's own hypothesis — "pre-existing precision gap" — was flagged for independent reviewer investigation, per this stream's established discipline of verifying claims rather than trusting reports).

The reviewer's decisive test: ran the *identical* `dryrun.test.ts` against the *identical* fixtures on the untouched `feat/real-data-dry-run` branch — it passed cleanly, confirming a genuine regression in this stream's own port, not an inherited or inherent tolerance question. Three rounds of real root causes were found and fixed, each closer to the true chain than the last:

- **Round 1** (a real but narrow bug, fixed, zero mismatches resolved by it): `export.ts` and `migrate-from-sheet.ts` independently reimplemented the same pooled-container SKU-matching predicate with different strictness, a latent freight/duty mis-attribution risk on any shipment whose raw ref coincidentally matched the pooling pattern for a SKU not actually on that row. Fixed (commit `1bc90ef`), proven via an A/B stash test — and proven, via the same methodology, to explain none of the 10 observed R4 mismatches.
- **Round 2** (the real cause of the 10 mismatches, but incompletely fixed): `poLineItems.unitPrice` was `decimal(18,4)` while real Sheet data (`Full Factory Cost/unit`) carries 6–7dp — MySQL was silently truncating on insert. Widened to `decimal(18,6)` (commit `624a672`) matching Stream J's own established per-unit-cost precision policy, then found still insufficient once an opus review ran the dry-run itself rather than trusting the fix report: real source data carries **7dp** (`1.1104994`), so `(18,6)` still truncated it, passing R4 only by a ~2.5× tolerance margin (not a real fix) — and, more importantly, unblocking the migration gate let **R3 run for the first time**, surfacing 39 previously-invisible `openingValue` mismatches from the same bug class one hop downstream in `inventoryLedger.unitCost`. Widened both columns to `decimal(18,8)` (commit `6a81310`) — confirmed by a second independent opus review (which ran the dry-run itself again) to resolve all 39 R3 mismatches and reproduce the original branch's own 2026-09-19 historical target shape almost exactly.
- **Round 3** (closing the chain): the same review found a third, tighter-margin instance of the identical bug class — `shipmentLineItems.weightShare`/`valueShare` (`decimal(9,6)`) feeding the same R4 formula at only a 1.29× real margin, with a demonstrated failure boundary (any pooled-container line under ~10,100 units would exceed tolerance). Widened to `decimal(9,8)` (commit `9754b55`). A third independent opus review computed real margins for every remaining candidate column rather than trusting the sweep was complete by inspection (R4 path: ≥58× margin; R3 path: ≥8.75× margin; `fxRate` columns: 0 measured error across all 27 real fixture rows) and gave a decisive **chain-closed** verdict — no 4th round needed. One Low doc-accuracy finding fixed directly (commit `da65ebf`): the design doc's original precision-selection rule measured headroom against what the code happened to write, not against real source-data precision or the tolerance actually consuming the value — corrected so a future column addition doesn't repeat the mistake.

Total: 3 fix rounds + 1 doc correction, 3 independent opus reviews (each one ran the dry-run itself rather than trusting the previous report), resolving what began as "1 failing test in Task 8" into 3 separate real under-scaled `decimal` columns that would have silently truncated real Jello financial data had this stream not re-validated against real data before a cutover decision.

## Task 10: the real re-run

Ran `pnpm dryrun` (no flags — live Sheet, sheet id `1pSVrpDwiN6Ja2RfwVbsxj4H6J3eBRtnLtKNc1TGtoMk`, gviz-readable) against a fresh local `accommerce_dryrun_test` database on 2026-09-21. Full committed result: `docs/dryrun/2026-09-21/report.md`.

**Result: R1 6/0, R2 588/0, R3 582/0, R4 24/1, R5 22/0, R6 3/0, R7 6/0, Q 0, L 0 — verdict `SAFE TO CUT OVER`** (1 item needs a decision, 0 items block the platform). 20 paid payments transferred, 20 transaction links transferred (4 with a noted 1.2–4.8% variance, all within the decided tolerance tiers), 0 quarantines, 0 untransferable links.

The single R4 mismatch (`Mutual-PO2-Delivered / Mixer qty`, Sheet 3240 vs. platform 700, diff −2540) is the same already-classified Sheet-side data-entry bug the precision-bug chain's fixture-replay validation found (the Landed Cost Summary tab restates the Straw row's quantity onto the Mixer line; the Sheet's own Apps Script lookup keys on Shipment ID only, not Shipment ID + SKU) — not a platform defect. R2/R3's higher check counts vs. the 2026-09-19 fixture (576→588, 570→582, +12 each) are exactly the 2 additional real sales days that occurred on the live Sheet between the fixture's capture date and this re-run — expected, not investigated further, per the plan's own instruction not to force a match against stale numbers. No genuinely new, unexplained finding appeared.

## Final Review (Stream L)

A whole-branch review (opus, across the full 20-commit range) independently re-ran the live dry-run on the stream's own final code and got a byte-identical report to the one already committed — direct confirmation Task 10's result was genuine, not stale or copied. All 9 adaptation points were re-verified against final code, several against real migrated data rather than just code shape: the decimal-precision chain confirmed coherent end-to-end across schema and both live databases; Task 6's `matchTransactionToPayment`-bypass judgment confirmed correct at the data level (18 real payments carried both a Sheet `paid: true` and a `matchedRef`; the change log showed exactly 20 paid-field rows and zero double-writes); the pooled-container merge hand-traced against real Container 2 data; the variance-tier rule confirmed structurally consistent between the migration and R5 (the migration never touches `expectedAmount`, the exact field R5 compares, so the two paths cannot disagree on it).

The review found 4 findings, all latent paths the day's real data didn't exercise but real enough to close before relying on the tool's ongoing daily parallel run: (1) payment-side pooling lacked the SKU cross-check the shipment side already had, risking silent freight/duty mis-attribution if the Sheet ever produced a coincidentally-matching-but-wrong container ref; (2) R5's reconciliation check derived a payment's owner ref via a different code path than the actual migration, risking a false "not safe" verdict component on an otherwise-correct migration; (3) a non-EUR transaction with a blank FX-rate source cell produced an empty string that would crash the entire migration transaction under MySQL strict mode rather than quarantining the one bad row — confirmed close to triggering, since the live Sheet already had a near-miss row for this condition; (4) two stale `.mjs`-referencing comments in Task 8's CLI scripts.

All 4 were fixed in one round (commit `b34628e`): (1) `transformShipments` now returns the set of genuinely-pooled owner refs, and payment pooling only pools against that real set instead of a bare regex guess; (2) a single `resolveShipmentOwnerRef` function was extracted and is now imported by both the migration and R5, eliminating the two-implementations-of-one-concept pattern that had already caused a similar bug earlier in this stream (`export.ts`/`migrate-from-sheet.ts`'s `platformShipmentRef` divergence, fixed in commit `1bc90ef`); (3) the malformed-FX-rate row is now quarantined via the same `skipped.push` pattern this file already uses for other malformed rows, instead of reaching the database write. A scoped opus re-review independently confirmed all 4 fixes correct — including tracing a concrete non-pooled-payment scenario end-to-end through a real-DB test, confirming the shared-function fix resolves to one single definition (not two functions with the same name), and independently re-running the live dry-run to confirm a byte-identical report to the pre-fix version (direct evidence the fixes suppressed no real mismatch and introduced no regression). Two non-blocking minor observations were noted and left as-is (a test that structurally cannot fail since it shares the same fixed dependency it tests — the independent live re-run is the real guard against drift; a few other scripts outside this finding's named files still carry `.mjs`-referencing comments).

## Final State (Stream L)

- Test suite: 373 tests total, 372 passing (up from 232 at the end of Stream K). `pnpm check` (`tsc --noEmit`) clean. The 1 failing test (`dryrun.test.ts`'s own real-database fixture-replay case) is a pre-existing, environment-configuration condition, not a Stream L defect: it asserts against a database whose name ends in `_dryrun`/`_dryrun_test` by design (`assertDryRunDatabase`), while this checkout's committed `.env` points `DATABASE_URL` at `accommerce_dev` for normal development — noted, unfixed, since Task 9's own close-out, confirmed unrelated by both the final review and its re-review.
- The dry-run item in Backlog section B ("tolerance-based landed-cost reconciliation... not yet wired to a live data source") is now closed — see the Backlog Stream L section in `docs/BACKLOG.md`.
- Final verdict, confirmed live and reproduced independently three separate times (Task 10's own run, the final review's re-run, the fix round's re-review re-run — all three byte-identical against the live Sheet on 2026-09-21): **`SAFE TO CUT OVER`** — 1 item needs Artem's decision (a pre-existing Sheet-side data-entry bug, not a platform defect), 0 platform defects remain. Cutover itself is a separate, explicit business decision still gated on Artem — this verdict is a technical readiness statement, not a go-ahead.

## Backlog Stream M — Reversal / Correction Paths

Before this stream, no path existed anywhere in this codebase to correct a wrong receipt quantity, a wrong landed cost, or a wrong paid-payment amount except raw SQL — despite `inventory_ledger` being append-only by design and a real bug class (a `NaN`/corrupted landed cost silently and permanently written into the ledger) having already been found and fixed once during Stream I without ever answering "what happens if it happens anyway." Flagged repeatedly since Stream I's 2026-09-20 review as the top-priority Operational-lens deferred item, this stream closed it — the first live write path in this codebase that appends real rows into `inventory_ledger` outside the original one-time migration.

Full design in `docs/2026-09-21-reversal-correction-paths-design.md`; task-by-task plan in `docs/superpowers/plans/2026-09-21-reversal-correction-paths.md`.

**What was built** (9 tasks):

- **Schema**: 5 new nullable columns on `inventory_ledger` (`lineItemId`, `correctsEventId` — a genuine self-referencing FK, `changedBy`, `reasonCategory`, `reasonNote`) and a new `"data_correction"` reason category shared with `change_log`.
- **`correctLedgerReceipt`** (`server/inventoryLedger.ts`) — the core primitive. Appends an offsetting `adjustment` plus a replacement `receipt` for a wrong receipt, forward-only: never rewrites an existing row, never alters previously-computed Daily COGS for a past day. Guards, in order: event exists and is a receipt; not already corrected; the correction actually changes something (decimal-scale-aware, not a raw string compare); non-negative whole-unit qty and finite non-negative unit cost; the reversal doesn't drive SOH negative (bypassable via an explicit, per-call `allowNegativeSoh`); and — found and closed mid-task, not merely documented — a pre-commit self-check that replays the SKU's full ledger history before committing and refuses (full rollback) any correction that would newly leave it unreplayable, while still allowing one on a SKU whose history was already broken beforehand (a real backorder-instance case).
- **`correctShipmentReceiptQty`** / **`correctShipmentLandedCost`** (`server/shipments.ts`) — resolve a human-meaningful identifier (shipment + line item) to the right ledger receipt via `findUncorrectedReceipt` (`lineItemId`-first, `skuId`-fallback for legacy rows), correctly disambiguating the case Stream G had already found real (two line items on one shipment sharing a SKU). The cost-correction path restates freight/duty and corrects every affected line's receipt in one all-or-nothing transaction, skipping (not failing on) any line whose recomputed cost happens to be unchanged.
- **`correctPaymentAmount`** (`server/payments.ts`) — a precondition-checked, explicit correction entry point for an already-paid payment, delegating to the existing `markPaymentPaidCore`.
- **3 tRPC procedures**, with new zod-level decimal-string validation scoped only to these 3 procedures (a real pre-existing gap on money fields elsewhere in this router was not retroactively fixed — out of scope).
- **2 UI controls** (`ShipmentsPage.tsx`, `PurchaseOrdersPage.tsx`'s `MarkPaidRow`) — no `reasonCategory` dropdown anywhere (every correction is `"data_correction"` by construction), neutral copy throughout, and a progressive-disclosure `allowNegativeSoh` "Force this correction anyway" escape hatch that only appears after a real refusal naming it.

**Mid-stream findings, all fixed, none deferred**: Task 3's review escalation closed a narrow-but-real no-op-guard gap (sub-scale decimal precision could slip past the "did anything actually change" check) and an unvalidated blank `reasonNote`. Task 4 found and fixed a real bug in the plan's own `findUncorrectedReceipt` text (a `correctsEventId IS NULL` filter that would have made a second correction of an already-corrected line item silently impossible) and a real cross-file test-isolation trap the new self-referencing FK introduced (fixed for `shipments.test.ts`; `scripts/reconcile-migration.test.ts`'s own instance of the same trap was fixed in Task 5). Task 5's own review found that a shipment-wide cost restatement was refused *wholesale* the moment any single line's cost happened to be unchanged — fixed (skip only that line) across 2 fix rounds, the second closing a real test-coverage gap the first round's own regression test didn't actually cover. Task 8's `allowNegativeSoh` UI escape hatch initially only caught one of two real backend refusal messages the same parameter bypasses — found via live verification against a real dev server, not just code review, and widened.

## Final Review (Stream M)

A whole-branch review (opus, across the full 13-commit range) confirmed the forward-only invariant holds across the *entire* codebase, not just the individually-reviewed diffs — exactly one insertion point into `inventory_ledger` exists anywhere, zero update/delete paths outside test fixtures. The `lineItemId`-disambiguation chain was confirmed genuinely intact end-to-end, including two non-obvious cases no single task's review could fully see on its own: a second correction correctly targeting the first correction's replacement receipt, and a legacy null-`lineItemId` receipt's replacement correctly inheriting the null so the `skuId` fallback keeps working. Two claims the task-level reviews had explicitly deferred to this final pass — `correctShipmentLandedCost`'s all-or-nothing guarantee at 3+ line items, and `consumedFromOtherBatches`'s correctness across 3+ batches — were independently constructed and live-verified by the reviewer themselves, both holding.

4 findings, all in the presentation/validation/audit-trail layer — explicitly assessed by the reviewer as unable to corrupt data, none touching the ledger-write core: (1) the `allowNegativeSoh` force-button's detection regex also matched Task 7's own ordinary decimal-format validation error, meaning a plain input typo would surface the feature's one genuine safety-bypass button next to a scary integrity warning — a real risk of training operators toward the wrong habit; (2) `"data_correction"` remained selectable in 9 pre-existing, unrelated manual reason dropdowns across three pages, making a first-time write indistinguishable in `change_log` from a genuine correction — a gap Task 1's own review had already flagged and explicitly deferred to this exact triage; (3) the `allowNegativeSoh` warning copy said "this SKU" when a cost-path force actually applies to every affected line item on the shipment, and the consequence notice disappeared after a successful forced correction even though the real consequence persists; (4) `newQty`'s router validation wrongly rejected `0`, blocking `correctLedgerReceipt`'s own documented legitimate "this shipment never actually arrived" correction case — a plan-authoring bug, not an implementer slip.

All 4 fixed in one round: the force-button regex narrowed to the two real refusal message substrings (excluding the Zod message); a new `MANUAL_REASON_CATEGORIES` constant (excludes `data_correction`) now backs all 9 dropdowns and every pre-existing router procedure that accepts a `reasonCategory`; the cost-path warning reworded to reflect its real shipment-wide scope, with a persistent post-success notice added; `newQty` validation widened to accept `0`, with a real end-to-end test proving the case now works through the actual function. A scoped opus re-review independently live-verified the two regex changes against real captured error text from a running dev server (not just source reading), independently re-derived the full dropdown list via a repo-wide grep, independently confirmed via a live router call that `data_correction` is now genuinely rejected on a pre-existing procedure while a real manual category still succeeds, and confirmed zero new regressions across the widest-surface-area fix (the dropdown/schema change touching the most files).

## Final State (Stream M)

- Test suite: 409 tests total, 408 passing (up from 373 at the end of Stream L / Backlog section F). `pnpm check` (`tsc --noEmit`) clean. The 1 failing test is the same pre-existing, environment-configuration `dryrun.test.ts` condition noted since Stream L — unrelated to this stream.
- The "no reversal or correction path" item in `docs/BACKLOG.md`'s Operational (Cook) deferred list is now closed.
- Every finding surfaced anywhere in this stream — across 6 task-level fix rounds and 1 final-review fix wave — was fixed, not deferred. Nothing shipped with a known, unresolved correctness gap.

## Freight/Duty Cost Lock

`recordShipmentCosts` could be called to change a shipment's freight/duty at any time — before or after arrival. Once arrived, `markShipmentArrived` had already written `inventory_ledger` receipts computed from the costs recorded at that moment; a later `recordShipmentCosts` call updated the shipment's own displayed fields but never propagated to those already-written ledger rows, silently diverging the two with no error and no warning. Backlog Stream M (above) had already shipped `correctShipmentLandedCost`, a forward-only mechanism that fixes this *after* an operator notices — but nothing prevented or detected the divergence in the first place, and nothing pointed an operator toward the correction mechanism instead of the plain, ledger-unaware path. Flagged in Stream I's three-lens review as the top-priority remaining Operational-lens item, this stream closed it — chosen by Artem as the next design-pass item after Stream M.

Full design in `docs/2026-09-23-freight-duty-cost-lock-design.md` (brainstormed with 3 clarifying-question rounds: lock-timing, grace-window existence, grace-window ledger-safety); task-by-task plan in `docs/superpowers/plans/2026-09-24-freight-duty-cost-lock.md`. Executed subagent-driven, directly on `main` (matching every prior stream's established workflow in this repo — no isolated worktree).

**What was built** (4 tasks):

- **Schema**: two new nullable columns on `shipments` — `costsLockedAt` (timestamp), `costsLockedBy` (FK to `users.id`). Null means unlocked.
- **`applyShipmentCostChange`** (`server/shipments.ts`) — `correctShipmentLandedCost`'s existing ledger-correcting body extracted into a shared, module-private core. `correctShipmentLandedCost` itself becomes a thin wrapper (own signature, own error messages, own behavior all unchanged) hardcoding `"data_correction"`; the new caller below passes its own genuine category through instead.
- **`recordShipmentCosts`'s new branches** — pre-arrival: unchanged, zero ledger involvement. Post-arrival-unlocked: now calls the shared core, performing a real ledger correction (closing the actual desync bug), with `reasonNote` required at the value level even though the type keeps it optional for the pre-arrival case. Post-arrival-locked: refuses before any write, directing the caller to `correctShipmentLandedCost`. The shipment-level `change_log` entries keep the caller's real `reasonCategory`; the ledger-level correction (via `correctLedgerReceipt`, called internally) still always hardcodes `"data_correction"` — the two audit trails answer different questions (why the cost changed vs. that this was a forward-only correction) and this split doesn't collapse under the refactor.
- **`lockShipmentCosts`** (`server/shipments.ts` + `shipments.lockCosts` tRPC procedure) — one-way, no unlock function exists anywhere. Only possible once `status === "delivered"`; refuses a second lock; refuses a blank `reasonNote`.
- **UI** (`ShipmentsPage.tsx`) — a "Lock costs" button (visible only once delivered and unlocked), a 🔒 locked indicator with disabled freight/duty inputs and Save button once locked, a broadened `reasonNote` requirement on the plain cost form once delivered (matching the server's new value-level check), and — added mid-stream as a real, ruled-on gap rather than deferred — a "Force this correction anyway" affordance for `allowNegativeSoh` on the plain cost-save path, extracting the message-matching logic `CorrectReceiptControl` already had into a shared `needsForceBypass` helper both controls now call.

**Mid-stream findings, all fixed, none deferred**: Task 2's own review found one Important, test-only issue — the test proving the `reasonCategory` split (caller's category on `change_log`, hardcoded `"data_correction"` on the ledger) indexed positionally into a `changedAt DESC` list with second-precision timestamps, both flake-prone and non-discriminating (it would still have passed even if the split were implemented backwards). Fixed by selecting the row under test by content (its actual old/new cost values) instead of array position, matching this file's own established convention elsewhere. A ⚠️ item from the same review — the plain cost-save UI never exposed `allowNegativeSoh` even though the backend now threads it end-to-end, and a routine post-arrival cost edit can now trigger the same FIFO-replayability/negative-stock refusal `correctLedgerReceipt` always could — was ruled a real gap and folded into Task 4's dispatch rather than deferred to a future stream.

## Final Review (Freight/Duty Cost Lock)

A whole-branch review (opus, across the full 5-commit range) re-verified, against the final code rather than each task's own diff, that both `recordShipmentCosts` refusal paths (locked; blank `reasonNote`) genuinely throw before any write; that the extraction is behavior-preserving (hand-diffed the final `applyShipmentCostChange` against the pre-extraction body); that the `reasonCategory` split holds in both directions; and that neither `lockShipmentCosts` nor `recordShipmentCosts` reads a stale lock value (both read fresh inside their own transaction). It independently ran the full suite and `pnpm check` itself rather than trusting the task reports.

2 Important findings: (1) `recordCosts`'s tRPC procedure validated `freightCost`/`dutyCost` as plain unvalidated strings while the sibling `correctLandedCost` procedure already used `nonNegativeDecimalString` — tolerable before this stream when `recordCosts` was a plain field write, no longer tolerable once it performs a real ledger correction, since a malformed or negative value could now reach `inventory_ledger` instead of being rejected at the router boundary; (2) the plain "Save costs" form gave an operator no indication that saving on a delivered, unlocked shipment now performs a real ledger correction for every affected line — the only visible change was a `reasonNote` field appearing, with the first hint of "correction" semantics being a force-bypass button that only appears after a specific refusal. The review also escalated 3 previously-parked Minor findings for the same fix wave, since the context was still fresh: a message-prefix regression from the extraction (`correctShipmentLandedCost`'s "no shipment found" error had picked up the shared core's generic prefix, contradicting the plan's own "error messages unchanged" constraint — a plan defect, not an implementer slip), a dropped WHY-comment explaining the no-op-skip rationale, a stale router comment undercounting correction procedures (3 → 4 once `lockCosts` joined them), and locked-indicator copy that claimed a specific visual direction ("above") that wasn't accurate for this table's column layout.

All 6 fixed in one round (commit `8de6387`): `recordCosts` now validates with `nonNegativeDecimalString`; a new inline note explains the ledger-correction consequence once delivered-and-unlocked; both `correctShipmentLandedCost` and `recordShipmentCosts` restore their own original not-found message prefix (a small local check ahead of the shared core's generic one); the no-op-skip rationale is restored, reworded to fit the extracted function; the router comment now says 4 and names all four procedures; the locked-indicator copy drops the spatial claim. A scoped re-review independently confirmed all 6 addressed with no new breakage, verifying each against the actual code rather than trusting the fix report — including confirming the stricter `recordCosts` validation doesn't conflict with the client's own pre-submit guard, and that the restored error-message fix doesn't leave the shared core's generic message unexpectedly reachable from either caller.

## Final State (Freight/Duty Cost Lock)

- Test suite: 421 tests total, 420 passing (up from 411/412 — 410 passing — at the start of this stream, per `hot-accommerce.md`'s prior snapshot). `pnpm check` clean. The 1 failing test is the same pre-existing, environment-configuration `dryrun.test.ts` condition noted since Stream L — confirmed present identically on unmodified `main` via `git stash`, unrelated to this stream.
- The freight/duty item in `docs/BACKLOG.md`'s Operational (Cook) deferred list is now closed (split out from the bullet it previously shared with the still-open nightly-export row-bound item, which this stream does not touch).
- Every finding surfaced anywhere in this stream — 1 task-level fix round (Task 2) plus 1 final-review fix wave — was fixed, not deferred. A handful of genuinely low-stakes Minor observations (a pre-existing concurrency pattern with no row lock, widened in exposure but not introduced by this stream; a UI no-change guard; `costsLockedBy` currently write-only in the app; a stale doc-comment on `nonNegativeDecimalString` surfaced during the final re-review, already imprecise before this stream) were explicitly parked for a future backlog pass, not silently dropped — see the SDD ledger for the full adjudication if needed.

## Nightly Export Row-Bound & Table-Drift Fix

The last remaining Operational-lens item from Stream I's three-lens review, chosen and executed autonomously (Artem: "сам вирішуй, сам роби" — pick and build the next item yourself) rather than the freight/duty cost-lock's full brainstorm→design-doc→plan→SDD pipeline: a bounded technical fix, comparable in scope to the 09-22 cleanup batch, not a multi-day feature stream. Scoped via `quick-scope`, implemented directly with TDD in one session — no design spec, no implementation plan, no subagent dispatch.

`runNightlyExport` (`server/nightlyExport.ts`) loaded every `CORE_TABLES` table into memory as JS objects via one unbounded `db.select().from(table)`, then built one giant in-memory CSV string, then wrote it out in a single `writeFile` call — flagged in Stream I's review as an OOM risk at real ledger volume, never fixed.

While reading the file to scope the fix, a second, more concrete bug turned up: `CORE_TABLES` was still missing `sales_plan_weekly_inputs`/`sales_plan_weekly_recipe_lines`. `docs/BACKLOG.md` and this file both already claimed this exact gap "fixed" (commit `c8f99a3`, "fix nightly export table drift") — but that commit only added the two tables' *imports*, never actually added them to the `CORE_TABLES` object the export loop iterates over. Both tables had been silently dropped from every nightly export since Stream H introduced them, un-noticed because no test exercised `runNightlyExport` against a real database at all — only `generateCsvExport`, the pure CSV-formatting helper, had test coverage.

**What was built:** `exportTableToCsv`, a new function that streams one table to its CSV file in bounded pages (keyset pagination on `id` — every `CORE_TABLES` table has an autoincrement `id` primary key, so no table-specific logic is needed) via `fs.createWriteStream`, writing each page's rows as they arrive instead of accumulating the whole table or the whole output string in memory first. Default page size 5,000 rows; overridable per call, matching this codebase's existing `dbClient: DbClient = db` dependency-injection convention, which is what let the new tests exercise real multi-page pagination (5 seeded rows, page size 2, forcing 3 round trips) without needing to seed thousands of rows to prove the behavior. `CORE_TABLES` gained the two missing tables, bringing the real export count to 15.

`generateCsvExport`'s own CSV-escaping logic was extracted into two small shared helpers (`csvEscape`, `csvLine`) reused by both the pre-existing bulk function and the new streaming path, rather than duplicated — `generateCsvExport`'s own pinned tests pass unmodified, proving the extraction is behavior-preserving.

**Testing:** 3 new tests in `server/nightlyExport.test.ts` (the file's first tests to touch a real database): multi-page pagination produces output byte-identical to a single-query export of the same data; an empty table produces an empty file (matching the prior behavior exactly); `runNightlyExport` writes exactly 15 files including both previously-missing sales-plan-weekly tables — the direct regression test for the table-drift bug. Full TDD cycle: all 3 failed for the expected reason (`exportTableToCsv is not a function`; the drift test's `toContain` assertion failing against an 11-length list) before implementation, then passed. Full suite: 424 tests, 423 passing (the same pre-existing, unrelated `dryrun.test.ts` environment-configuration failure noted throughout this repo's history); `pnpm check` clean.

No design spec, no implementation plan, no SDD ledger exist for this fix — it was scoped and executed as one direct TDD pass, per the autonomous judgment call on process weight described above.

## Daily Parallel-Run Monitoring Fix & `feat/real-data-dry-run` Branch Deletion

Artem asked to check Control Tower Live's actual current data before deciding on production cutover, and to delete the `feat/real-data-dry-run` branch if it was no longer needed. Both surfaced the same real, previously-unnoticed gap.

`ops/com.scait.accommerce-parallel-run.plist` (a launchd job, 07:30 local, re-runs the full dry-run reconciliation against the live Control Tower Sheet every day) had been ported to point at `main` as part of Stream L's Task 8 — the repo's own copy of the plist file already said so. But the *installed* copy in `~/Library/LaunchAgents/`, the one launchd actually reads, was never refreshed from it. Every day since Stream L closed (2026-09-21), the job kept running against the superseded `feat/real-data-dry-run` worktree instead of `main` — silently, since its output (a macOS notification with the verdict) looked identical either way, and nothing compared which code path had actually produced it.

Re-installed the launchd job from the repo's own already-correct plist. The first manual run against `main` failed: `accommerce_dryrun`'s schema had drifted from `main`'s own migration history (built up over the worktree's separately-diverged migration set), so `drizzle-kit migrate` couldn't apply cleanly. `resetDatabase()` (`scripts/control-tower/dryrun.ts`) only deletes rows, not schema — insufficient here. Fixed by dropping and recreating the `accommerce_dryrun` database entirely, letting it rebuild fresh from `main`'s migrations on the next run.

A clean run against `main`'s own code confirmed **`SAFE TO CUT OVER`, unchanged** — the same single known Sheet-side bug (Mutual-PO2-Delivered / Mixer qty), no new mismatches. Combined with the worktree's own preserved 09-22/09-23 reports (backfilled into `docs/dryrun/` before deletion, `*.json`/`delta.md` intentionally excluded per `.gitignore`), this gives 4 consecutive days (09-21 through 09-24) of independent confirmation the verdict holds, the last one verified against `main`'s actual current code rather than the stale worktree.

With the daily monitor now genuinely pointed at `main` and its history preserved, `feat/real-data-dry-run` (`git worktree remove` + `git branch -D`) was deleted — its content had already been fully ported onto `main` by Stream L; nothing on it was still needed.

## Control Tower Field-Parity Initiative

Artem's framing, after seeing the first real UI screenshots: a redesigned UI sitting on top of real data gaps isn't a sellable tool — new clients need genuine feature parity with the Sheet they're being asked to give up, not just polish. Audited every real Sheet column (`scripts/control-tower/snapshot.ts`'s `EXPECTED_HEADERS`, captured 2026-09-19) against the actual schema, agreed scope with Artem, shipped all 5 parts directly on `main`, "гоу до кінця" (go all the way through, no per-step check-ins).

**1. External reference links.** `contractLink`/`invoiceLink`/`addOnLink` on `purchaseOrders`; `quoteLink`/`invoiceLink`/`customsInvoiceLink` on `shipments` (`customsDeclarationLink` already existed in the schema but had no update path — wired up too). Plain text URL fields, Drive-link style — store a reference, not the file itself.

**2. Freight/duty + PO cost component breakdown.** The Sheet splits freight into Delivery + Admin Fees, and shows EUST/VAT (recoverable) separately from Duty (not recoverable) — the app had only two fields (`freightCost`, `dutyCost`) with everything blended. `shipments` gained `adminFeesCost`/`eustAmount`/`vatAmount`; the landed-cost formula (`getShipmentLandedUnitCost`) now nets recoverable EUST/VAT out of the unit cost, matching the one-time migration's own established convention. On the PO side, the Sheet splits EXW/Lab-Test/Inspection/Add-on per unit where the app had one blended `unitPrice` — `poLineItems` gained `exwUnitPrice`/`labTestUnitPrice`/`inspectionUnitPrice`/`addOnUnitPrice`, with `unitPrice` now recomputed server-side as their sum whenever any component changes. All new columns nullable and backward-compatible, proven by a dedicated test that an update touching only one component doesn't clobber the others.

**3. Factory production tracking.** `poLineItems` gained `qtyProduced`; `getPoLineItemProductionProgress` computes `qtyRemainingToProduce`/`qtyRemainingToShip` fresh from real `shipmentLineItems` rows every time, never stored — explicitly in scope per Artem ("наш скоуп"), overriding this codebase's own stated "Ops is out of scope" positioning for this one case.

**4. Smaller fields.** `purchaseOrders.actualReadyDate` (mirrors the existing `plannedReadyDate` pattern exactly); `shipments.shipMethod` (free text, deliberately unaudited — same tier as reference links). Found a bigger gap while implementing this part: shipments had no payments UI at all, despite the backend already supporting shipment-owned payments generically since Stream M — added `listPaymentsForShipment` + a minimal payments section on the Shipments page (customs fees, freight instalments).

**5. UI/UX polish — rescoped mid-task.** The original plan (approved via `AskUserQuestion`) was Tailwind + shadcn/ui. Packages were already installed before catching a real factual error: Stream K (`798e28d`/`635f959`, 2026-09-20) had already shipped a genuine, accessibility-reviewed design system (`client/src/index.css` — CSS custom properties, IBM Plex Sans/Mono, `prefers-color-scheme` dark mode) already in active use by Stock/Shipments/Purchase Orders. Installing a second, competing styling framework on top would have duplicated it and risked regressing already-reviewed work. Reverted the package installs cleanly (`pnpm remove` + `git checkout -- pnpm-lock.yaml`), told Artem the correction directly, rescoped to the actual gap: Home had no layout beyond a bare `<dl>` (added a `StatCard` KPI-card grid using the existing design tokens); Stock only showed one warehouse row at a time via a toggle (now shows every warehouse plus a combined Total row, per Artem's explicit ask — `а можемо на екрані поруч обидва склади і загальний дешборд`).

Along the way: explicit constraints from Artem carried through the whole initiative — single-warehouse-per-shipment stays as-is (`хай так і буде`, no multi-warehouse support), PPV metrics are out of scope (`PPV - ні`). Also fixed, found incidentally while running the full suite: a real pre-existing test-isolation bug in `server/_core/loginFlow.test.ts` — its `beforeEach` did a plain `db.delete(users)` with no FK-disable dance (every other test file in this repo already has one), causing intermittent full-suite-only failures when another file's last test left a `change_log` row referencing a user. Fixed by applying the same established FK-disable pattern.

Tests 423 → 447 across the initiative. `pnpm check` clean throughout.

## Planned-Shipment SOH Reconciliation Tolerance

Found live: Artem created a real new PO/shipment mid-session while the daily dry-run tooling was open. The next `pnpm parallel-run` rolled back the *entire* migration — not just flagged one mismatch — because the Sheet's own "Current On-Hand" total for that SKU/warehouse had prematurely counted the new shipment's units before it had even left the factory (`status: "planned"`, real example: `Mutual-PO3.1 Mixers`, qty 600, not yet departed). The internal reconciliation gate (`reconcileMigration` in `scripts/migrate-from-sheet.ts`) runs *inside* `runMigration`'s own transaction and throws on any mismatch, rolling back everything with it — a real Sheet-side quirk turning ordinary new-PO activity into a full outage of the daily safety check.

**First cut (wrong, corrected same day):** `computePlannedShipmentQty` summed still-`"planned"` shipment quantities per SKU/warehouse from the already-exported shipment rows, and `reconcileMigration`'s SOH comparison unconditionally subtracted this from the Sheet's claimed total before comparing. Verified against the live Sheet: the known `Mutual-PO3.1 Mixers` case now passed. But a *second* live re-run the same day — Artem had also created `PO3 Jello` (a different new planned shipment) — surfaced a case the fix got wrong: for that SKU/warehouse pair, the Sheet's own "Current On-Hand" total did *not* include the planned quantity at all, so the unconditional subtraction turned a previously-passing comparison into a new false mismatch. The premise ("the Sheet always inflates for a planned shipment") held for one observed case and didn't generalize.

**Corrected fix**, found by tracing the exact numbers rather than re-guessing: the gate now tolerates *either* explanation — the Sheet's raw total, or the total minus the known planned quantity — never forcing one over the other. Verified against the live Sheet a third time: both cases now pass. The internal gate's fix alone wasn't sufficient, though — a second, independent code path (`reconcile()` in `scripts/control-tower/reconcile.ts`, the outer R1-R7 comparison used for the full daily report, called only *after* `runMigration` succeeds) had its own separate, untouched SOH check and kept the daily verdict at `NOT SAFE TO CUT OVER` even once the gate itself stopped rolling back. Extended `ReconcileInput` with the same `plannedShipmentQtyBySkuWarehouse` option and the same either-explanation tolerance, wired from the same exported shipment rows `dryrun.ts` already has.

Chasing the remaining R3 (Daily COGS) gap this same investigation surfaced turned up a different, unrelated root cause: Inventory Ledger's "Mixer Cumulative Received" for the Mutual warehouse was inflated by a flat +600 across *every* historical batch — including ones landed back in June, long before the new planned shipment existed — traced to the same `Mutual-PO3.1 Mixers` shipment (confirmed by reading the actual `jello-sc-tables.gs` Apps Script source: `buildDailyCogs()`'s formula deliberately implements *retroactive* backorder pricing, matching this platform's own `computeFifoDailySeries` exactly, so the discrepancy isn't a formula mismatch — it's the same premature-inclusion bug corrupting a second column). Artem's call: this is Sheet data, not software — stop chasing it (`Причому тут це до нашого софта. Давай працювати над софтом.`) — not pursued further; likely self-resolves once the shipment actually departs.

Tests: 447 → 457 across this fix (both the gate and the outer-report tolerance, TDD throughout, each verified against the live Sheet — not just unit tests — before being called done).

## Money/Share tRPC Input Validation

`nonNegativeDecimalString` already existed and guarded some fields (freight/duty costs, the 3 correction procedures) but not others: `unitPrice` (PO line item create), `weightShare`/`valueShare` (shipment line items), `expectedAmount`/`amount`/`fxRate` (payments, transactions) were all still bare `z.string()` with no numeric check at all. Chosen by Artem specifically after redirecting off the Sheet-gate investigation — relevant because he'll be the one manually entering new shipments and payments into the app going forward, and a malformed value on these fields would otherwise reach a `decimal` column as `"NaN"` or silently corrupt a landed-cost calculation.

Exported `nonNegativeDecimalString` (this project has no tRPC-caller test harness, so the Zod schema itself is the unit under test — `server/routers.test.ts`, new) and applied it to the 4 previously-unguarded money fields. A new `shareString` adds an upper bound of 1 on top of the same check for `weightShare`/`valueShare` — a share above 1 is a realistic typo (`"1.5"` meant as `"0.15"`) that previously wasn't caught until `getShipmentLandedUnitCost` ran at cost-compute time, long after the shipment line was created, with a much less useful error. Sales-plan fields (`plannedRevenue`/`primaryPercent`/`unitsPer1000`) turned out to already be validated one layer down, inside `regenerateSalesPlanForWeek` — not a real gap, left as-is. Went through the `superpowers:brainstorming` bounded path (short design in chat, explicit approval) before implementation, per this repo's established process.

## Real-Data UI Audit

Artem: "Все продовжуй... Мені потрібен робочий веб апп з функціоналом та UI" (I need a working web app with functionality and UI, not just correct data underneath). `accommerce_dev` (the local dev database) was completely empty, so a throwaway demo instance was stood up pointed directly at `accommerce_dryrun` (a live copy of the real migrated Jello data, already sitting there from the day's parallel-run) — separate API process on `:3099`, separate Vite instance on `:5174` proxying to it, a reset password on the existing `migration@accommerce.system` system user for login. This produced the first-ever render of these pages against realistic data volume, since this codebase has zero frontend automated tests by established convention — verification here is always a real click-through, not a snapshot test.

Four real bugs found and fixed, each verified against the live demo before and after:

1. **Purchase Orders (11 real rows) rendered 10,120px tall; Shipments (24 rows) rendered 21,128px.** Every row's full detail — payments, line items, cost-component forms, links, status controls — rendered unconditionally, all the time, with no collapse. Both pages restructured to a compact one-line summary row (status badge, a few key figures) with a "Details" toggle revealing the exact same controls that were always there, in a second full-width table row that only renders when expanded. No functionality removed — verified the expanded state still exercises every control (customs, receipt correction, costs, links, payments) correctly. Result: both pages now fit in 1,600–2,770px against the same real data.
2. **Transactions (167 rows, unbounded query) rendered 14,282px.** `listTransactions()` has no server-side limit and the page rendered every row. Fixed client-side (the already-fetched list sliced in the browser, not a real cursor-paginated query) — shows the first 50 (most recent, since `listTransactions` already orders `desc` by date) with a "Show N more" button. Deliberately not a server-side fix: sufficient at today's real volume, a genuine fix would be its own scoped piece of work if this grows by an order of magnitude.
3. **Stock's combined-warehouse Total row used the wrong status thresholds.** `combinedRow()` in `StockPage.tsx` recomputed the Total row's `critical`/`low`/`ok`/`overstock` status with hardcoded day-of-cover bands (`<14`/`<30`/`>120`) that predate Stream K's per-SKU `leadTimeDays`/`safetyStockDays` reorder-point calculation — its own doc comment claimed it matched the backend, which stopped being true the moment the backend moved off fixed thresholds. A real Jello SKU (135.4 days of combined cover) read as `OVERSTOCK` (>120) when its actual reorder point (66+14=80, "ok" up to 80×3=240) said it was just `OK`. Fixed at the source: `getStockDashboard()` now computes each SKU's combined total server-side using the exact same `getStockStatus()` the per-warehouse rows already use, returned as a new `total` field per row — the threshold logic now lives in exactly one place, and the client just reads it instead of reimplementing it. Verified live: the same real SKU now reads `OK`.
4. **Landed Cost tab showed a raw internal database id, not anything meaningful.** `MoneyPage.tsx`'s Landed Cost table rendered `row.lineItemId` directly (e.g. `254`) in a "Line item" column meant to disambiguate rows when a shipment carries multiple lines of the same SKU. Fixed entirely client-side — fetches the selected shipment's line items (already available via the existing `shipments.getWithLineItems`) and shows each row's real `qty` instead. Deliberately did *not* touch `getShipmentLandedUnitCost`'s own return shape to add this — that function is a safety-critical, heavily-tested core (`recordShipmentCosts`, `markShipmentArrived`, the migration reconciliation gate all depend on it), and widening its return type would have rippled through several `toEqual`-style tests for a purely cosmetic gain.

Also used the same real data to sanity-check the 66/14-day reorder-point defaults themselves (a separate, previously-deferred backlog item, distinct from bug #3 above): 2 of 6 real SKU/warehouse pairs read critical (both Mutual, genuinely low), 3 ok, 1 overstock — reasonably differentiated, not "most of the catalog reads as at-risk" the way the original concern worried. No change made; closed as verified, not a defect.

Tests 447 → 467 across this pass (bug #3's server-side fix got a dedicated multi-warehouse test; the other three are client-only, verified by the established manual-browser-pass convention, not new automated tests).

## Schema-Derived `nightlyExport` Table Set

The earlier Nightly Export Row-Bound fix (above) had already found and closed one instance of `CORE_TABLES` silently dropping tables from every export. Revisiting it as its own small, bounded fix: `CORE_TABLES` was still a hand-picked object (table name → Drizzle table) plus a matching block of named imports — two places that had to be kept in sync by hand, which is exactly the failure mode that already bit once for real. `deriveCoreTables` now builds the export set from every real Drizzle table in the schema module, verified via `drizzle-orm`'s `is()` brand check (so an exported enum array, type, or helper function can't be mistaken for a table), keyed by its actual SQL table name, minus an explicit exclusion set (`users` — carries password hashes, must never leave via a data export; `app_settings` — internal operational config, not client business data, preserving the export's existing effective scope rather than silently changing what gets included). A new table added to the schema in the future is picked up automatically; there's nothing left to remember to update in this file. Verified with 4 new unit tests against small real (not mocked) Drizzle tables built via `mysqlTable()`, proving the inclusion/exclusion/filtering logic directly rather than only re-checking today's fixed table count.

## Production Deployment — GitHub Org Migration + Railway + TiDB Cloud

The platform's first real production deployment. Artem: "Треба мабуть спочатку 3 потім 2 потім?" (sequencing Railway hosting before Shopify automation before the cutover decision itself) — reasoned through together: Railway is the actual place production would run, Shopify's daily sales-data pipeline is a real functional dependency once the app is source-of-truth (not just "nice to have"), so the practical order is infrastructure → integration → the cutover decision. Both later steps stay blocked on external access Artem doesn't unilaterally have (Shopify: someone with store-admin access needs to create a custom app; Railway: only needed the browser signup itself, which did proceed).

**New GitHub home.** Artem: products that already work should live under a real SCAIT-branded home, not a personal automation account (`Artem-SCM-AI`, the account this repo had been pushed from all along). Created `github.com/SCAIT-agency` as a proper GitHub Organization (recommended over another personal account — supports team access, not tied to one login). First repo-name attempt (`apps/online/accommerce-inventory`) hit a real GitHub limitation — repository names don't support `/` as a folder separator, it just gets flattened to `apps-online-accommerce-inventory` — corrected to the plain, already-everywhere-referenced `accommerce-inventory`. Granted `Artem-SCM-AI` Write access on the new repo. Pushing the full existing history required a genuine judgment call around this session's sandbox: a force-push (to overwrite the org's auto-generated placeholder README commit) was explicitly approved by Artem but still categorically blocked by the environment's own auto-mode classifier, which does not appear to honor an in-conversation confirmation for that class of action — rather than working around the block, asked Artem to delete and recreate the repo without the auto-README, making a *plain* (non-force) push sufficient once the target repo was genuinely empty. A second block (any push to a not-yet-established remote, even without `--force`) required Artem to run the final `git push` himself in his own terminal, using a `gh auth switch --user Artem-SCM-AI` first — this environment's git credential helper otherwise defaults to the session's other configured identity (`ArtemTucann`), which doesn't have Write on the new org. Local remotes end up as: `origin` (the old `Artem-SCM-AI/accommerce-inventory`, kept, untouched) and `scait-agency` (the new canonical one, pushed to going forward).

**TiDB Cloud.** New Starter-tier (free) cluster under the SCAIT-agency org, Frankfurt region (close to the DACH market this platform's first real client operates in). The connect dialog's "Database" field only offers already-existing databases (`sys`, `test` by default) — not free text — so the real target database (`accommerce`) had to be created via a direct `mysql` client connection before it could be selected there. `pnpm exec drizzle-kit migrate` applied the full migration chain directly against the new production database (not `db:push`, matching `RAILWAY.md`'s own explicit warning about that distinction) — all 18 tables confirmed present via `SHOW TABLES`. TiDB Cloud's Starter tier has no visible Backup settings in its own UI (Settings only shows Networking) — flagged as unresolved rather than guessed at; not a blocker for today's deploy.

**Railway.** New project connected to `SCAIT-agency/accommerce-inventory` via GitHub. First deploy failed outright at `pnpm install --frozen-lockfile`: `ERROR packages field missing or empty`. Root cause: `pnpm-workspace.yaml` had only an `allowBuilds` block (approving `esbuild`'s install script) with no `packages` field at all — this never surfaced locally, where `node_modules` is already populated and installs are incremental, but a genuinely clean, frozen-lockfile install (exactly what Railway's build does, and what a fresh clone always needs) fails on it outright. Fixed by adding `packages: ["."]` — this is a single-package repo, not a real workspace; `allowBuilds` was the only reason the file existed at all. Verified against a fresh `git clone` before pushing (both `pnpm install --frozen-lockfile` and `pnpm build` from scratch), not just asserted.

Environment variables (`DATABASE_URL`, `SESSION_SECRET` — generated via `openssl rand -hex 32`, `PORT=3000`) set through Railway's own suggested-variables UI, which auto-detects variable names from `.env.example` but pre-fills them with that file's placeholder values — caught before saving that the suggested values were the dev-local defaults, not the real production connection string, and replaced them with the actual TiDB URL and generated secret before confirming. First production user created via the existing `scripts/seed-first-user.ts` bootstrap, run directly against the production `DATABASE_URL` (`ops@accommerce.example`) — the first password generation was lost to an inline, never-echoed shell substitution; regenerated immediately via `scripts/reset-password.ts` and displayed properly the second time. Public domain generated (`accommerce-inventory-production.up.railway.app`); full login flow verified end-to-end against the live production URL via a real headless-browser run, not just an HTTP 200 check — confirmed `/api/auth/login` succeeds and the dashboard renders real (in this case, correctly empty — a fresh production database, no data entered yet) data.

Both generated secrets (`SESSION_SECRET`, the seeded user's temporary password) passed through this conversation's chat transcript at some point during setup — flagged to Artem per this workspace's standing security-hygiene rule; the session password especially should be changed by whoever actually uses that account.

**Historical data backfill — deliberately not done yet.** Artem asked how the real historical Jello data would get into this now-live production database. Answer: the exact same migration tool (`scripts/run-migration.ts`) already exercised dozens of times against `accommerce_dryrun` throughout this whole session — the only difference is writing to the real `accommerce` database on TiDB instead of the dry-run one. Explicitly framed as its own separate, deliberate step (effectively the cutover itself, not a small follow-on task) — not run today, waiting on Artem's own explicit go.

## Design System Retheme — Tucann-Derived Palette

Artem, after seeing the freshly-deployed but empty production instance: "А якщо ми візьмемо дизайн у Тукана? Тільки кольори поміняємо?" (what if we take Tucann's design, just change the colors?). Investigated Tucann's actual `client/src/index.css` (a separate SCAIT client codebase) before answering rather than guessing: Tucann runs Tailwind v4 + shadcn/ui conventions (oklch color space, `--card`/`--popover`/`--sidebar` token names) — a genuinely different frontend framework from this app's own plain-CSS, CSS-custom-property design system (the one from Stream K, already reused/preserved once today during the Control Tower field-parity UI-polish rescope). Literally porting Tucann's design would mean re-platforming every page's styling, not "just colors," and would risk regressing the already accessibility-reviewed system in place.

Presented the real distinction to Artem (via this session's first use of the `superpowers:brainstorming` visual companion, a local browser-based mockup tool) as two options: (A) port Tucann's actual framework, or (B) keep this app's own token-based CSS architecture and re-theme its token *values* toward Tucann's hue family. Artem chose B. A follow-up question (dark-only, matching Tucann's own design, which has no light mode at all — vs. keeping this app's existing light+dark split) was resolved in favor of keeping both.

The actual palette was lifted directly from Tucann's own CSS file — a "Dark Ops" design-system comment at the top names its exact hex values (deepest `#0A0F1C` / mid `#131B2E` / elevated `#1C2640` surfaces; status red `#EF4444`, amber `#F59E0B`, emerald `#10B981`, cyan `#06B6D4`) — reapplied onto this app's own `--ink`/`--surface`/`--accent`/status-color token names for dark mode, with an analogous light-mode palette derived in the same hue family (Tucann has nothing to reference there, since it has no light mode). Every foreground/background pair — including badge text-on-soft-background combinations, not just the obvious body-text cases — was checked against a real WCAG luminance-contrast calculation (not eyeballed) before finalizing; two pairs (critical-text-on-critical-soft-background, both modes) needed their soft background tuned lighter/darker to clear the 4.5:1 AA threshold. Verified visually against real data, both color schemes, on Home and Stock (including the status badges) via headless-browser screenshots before committing.

No component or page markup changed — same CSS file, same token names, only the color values, exactly matching what "just change the colors" turns out to mean once the two design systems' actual architectures are understood.
